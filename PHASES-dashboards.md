# PHASES — فرع feat/dashboards

> يُدمج في `PHASES.md` من جلسة `main` عند اكتمال الفرع (docs/11 §التوثيق).
> لا تعدّل `PHASES.md` من هذا الفرع.

## المرحلة 3.2 — لوحات التحكم ☑ (2026-09-02)

**السياق:** عملاء من خلفية الجزيرة يحتاجون رؤية وثقة لا مرونة فعلية
(docs/08 §المبدأ). الشفافية تُشترى؛ الانتظار المعروف مقبول والمجهول مرفوض.

**النطاق:** طبقة قراءة + لوحة عميل + لوحة تشغيل + إجراءات إدارية +
بذرة تنبيهات + نظام لغات (ar/mixed/en).

### الملفات الجديدة

- `apps/renderer/src/observe.ts` — طبقة قراءة خالصة. لا تعديل على
  `queues.ts` أو `worker.ts` (قيد فرع dash: مقفلان على main).
  - `queueDepth()` — عمق كل طابور مع `paused` و `concurrency`.
    **يجمع BullMQ 5 `prioritized + waiting` تحت `waiting` واحدة**
    (L-dash-2).
  - `activeJobs()` — المهام النشطة مع `ageMs` و `progress`
  - `jobPosition(jobId)` — موقع + `expectedStartSec` (الصيغة أدناه)
  - `tenantJobs(tenantId)` — كل مهام مستأجر عبر الطوابير
  - `failureRate(hours)` — نسبة الفشل + تصنيف الأسباب بـregex
  - `resourceUsage()` — معالج/ذاكرة/قرص (df -k -P)
  - `tenantDistribution()` — توزيع بين العملاء (يُقاطع مع عدّاد Redis)
  - `systemStatus()` — يعيد `status` + `reasonKey` (مفتاح i18n لا نص)
- `apps/renderer/src/observe.test.ts` — 5 اختبارات vitest تحرس L-dash-2:
  خليط prioritized/waiting، مواقع تعبر priority. تعمل ضد Redis حقيقي
  ببادئة اختبار معزولة، تندمج مع `pnpm test`.
- `apps/renderer/src/alerts.ts` — واجهة webhook مع de-dup في Redis
  (SET NX EX). العتبات: قرص>80% · طابور>10 · عامل معلّق>5د · فشل مهمة.
  لا تكامل تيليجرام (webhook تكفي).
- `apps/dashboard/` — Next.js 14 على منفذ 19030
  - `/client` — لوحة عميل. تعرض: «مهمتك رقم N — تبدأ خلال Xث» أو
    «مهمتك تعمل الآن — Y٪». Polling كل 3 ثوانٍ. **لا بطاقات
    placeholder** — «سجل التصديرات» و«الاستهلاك» أُخفيا حتى تُتاح
    بيانات حقيقية (المرحلة 4).
  - `/ops` — لوحة تشغيل. أعمدة الطوابير + المهام النشطة + الفشل +
    الموارد + التوزيع + عتبات + إجراءات.
  - `/api/client`, `/api/ops`, `/api/ops/action` — REST خفيف.
- `apps/dashboard/src/i18n/` — نظام لغات خفيف بلا مكتبة (كائن مفاتيح +
  Context + hook):
  - `ar.json` · `mixed.json` · `en.json` — قواميس مسطّحة بمفاتيح
    متداخلة (`ops.col.name` إلخ)، مع تدعيم interpolation
    (`{pct}` · `{n}` · `{eta}`).
  - `LocaleProvider.tsx` — Context + `t()` + تسلسل الأولوية:
    `?locale=X` (اختباري) → localStorage → `ar` افتراضياً.
    يضبط `<html dir/lang>` عبر effect.
  - `LocaleSwitcher.tsx` — ثلاث أزرار في رأس الصفحة.
  - `Ltr.tsx` — يغلّف مركّبات أرقام بـ`dir="ltr"` لتنجو من قلب RTL
    (مثال: `108.0 / 460.4 GB`).
- `apps/dashboard/src/ui/AppHeader.tsx` — رأس مشترك يترجم العنوان
  والتنقّل.
- `scripts/dashboard-eta-check.mjs` — بوابة دقة التقدير.
- `scripts/dashboard-screenshot.mjs` — لقطة سِيَر واحدة (سياق العرض).
- `scripts/dashboard-locales-screenshot.mjs` — لقطة لكل من الأوضاع
  الثلاثة عبر `?locale=X`.

### أوضاع اللغة الثلاثة — القاعدة الحاكمة (المصححة 2026-09-02)

القاعدة الأولى («التقنيات إنجليزية والواجهة عربية») كانت خاطئة — أنتجت
تركيباً ركيكاً بالخلط داخل الجملة الواحدة: «مهمتك في الـ render»،
«حفظ (يتطلب restart)»، «على cores 10». تُنظر L-dash-6.

**القاعدة المصححة:** الخلط على مستوى **الوحدة الدلالية المستقلة**،
لا داخل الجملة.

**يبقى بالإنجليزية في mixed (كوحدات قائمة بذاتها فقط):**
- أسماء الطوابير: `Urgent · Normal · Edit · Batch`
- حالات المهمة (كتصنيف تقني): `Active · Waiting · Completed · Failed · Delayed`
- أعمدة تقنية بحتة: `ID · Template · Tenant`
- أسماء المتغيرات: `WORKER_COUNT · jobId`
- الاسم التجاري: `Media Kit — Dashboards`

**يبقى بالعربية كاملاً في mixed (بلا كلمة إنجليزية داخله):**
- كل الجمل والعبارات: «مهمتك قيد التنفيذ — 0%»
- عناوين البطاقات: «الموقع من كل عتبة» · «التوزيع بين العملاء»
- الأزرار: «حفظ (يتطلب إعادة تشغيل)»
- التذييل: «تحديث كل 3 ثوانٍ · آخر تحديث 12:01»
- حالة النظام كاملةً: «حالة النظام: عادي — كل شيء على ما يرام»
- «على 10 نوى»

**الفرق الجوهري:** الجدول قد يعرض `Active` في خلية الحالة (وحدة
مستقلة). لكن الجملة الافتتاحية تقول «مهمتك قيد التنفيذ» (عربية
كاملة)، لا «مهمتك Active». الأولى وحدة مستقلة؛ الثانية خلط داخل جملة.

**مقياس القبول:** اقرأ كل نص بصوت عالٍ. إن بدا هجيناً أو ركيكاً، فهو
خاطئ. الجملة إما عربية كاملة أو مصطلح إنجليزي مستقل — لا بينهما.

### الحفظ — الآن ولاحقاً

- **الآن:** localStorage (`pfmk.dashboard.locale`) + `?locale=X`.
- **المرحلة 4 (متطلَّب مسجَّل):** الحقل `brand.locale: 'ar'|'mixed'|'en'`
  في `packages/shared/src/brand-kit.ts`. يُحفظ مع هوية العميل فيراه كل
  مستخدميه؛ localStorage يبقى override يدوي على مستوى المتصفح.
  يستلزم:
  - إضافة الحقل لـ`BrandKit`، القيمة الافتراضية `'ar'`.
  - `apps/dashboard` يقرأ `brand.locale` (عبر `/api/client` مثلاً)
    ويستعمله كافتراضي بدل ثابت `'ar'`.
  - LocaleSwitcher يتحول من «قرار متصفح» إلى «قرار brandKit».

### تنسيق الأرقام في RTL

مركّب «108.0 / 460.4 GB» يُعكَس ترتيبه في سياق RTL (المتصفح يقلب موقع
«/» و «GB»). الحل: مكوّن `<Ltr>` يغلّف بـ`dir="ltr"`. مستقل عن اللغة —
الأرقام لاتينية في كل الأوضاع.

**أُطبِّق على:** حِمل المعالج (1.80 / 1.79)، الذاكرة (22.8 / 24.0 GB)،
القرص، عتبات (23.5 / 80)، نسب مئوية (0.0%)، jobId العدديّ، «0%» في
عمود التقدّم.

### صيغة التقدير (docs/08 §لوحة العميل — «الانتظار المعروف»)

مع `c` عمّال متوازيين ومتوسط زمن معالجة `avgMs`:
```
expectedStartSec = floor((position - 1) / c) * avgMs / 1000
```

`avgMs` مُشتق من آخر 20 مهمة مكتملة لكل طابور. عند غياب المكتملات،
افتراضيات محافظة: urgent=3s · normal=5s · edit=60s · batch=30s.

### الإجراءات الإدارية — ما هو ممكن بلا تعديل worker.ts

قيد فرع dash يمنع لمس `worker.ts`، لذا:

| الإجراء | يعمل | ملاحظة |
|---|---|---|
| حذف مهمة (waiting/delayed) | ✅ | `Queue.getJob(id).remove()` |
| حذف مهمة نشطة | ❌ | يتطلب token من العامل — نُرجع 409 |
| إيقاف/استئناف طابور | ✅ | `Queue.pause()/resume()` — يمنع سحب مهام جديدة |
| تفعيل/إنهاء الصيانة | ✅ | علم Redis + pause كل الطوابير |
| تعيين `WORKER_COUNT` | ◐ | يُخزَّن في Redis؛ يتفعّل عند إعادة تشغيل العامل يدوياً |

### البوابات — مُتحقّقة (2026-09-02 بعد rebase على main)

**كل الاختبارات (`pnpm test`):** **273/273 vitest** (زيادة عن 253 على main
بسبب: 15 اختبار timeline-v2 جديدة من main + 5 اختبارات observe الجديدة).
`pnpm verify:snapshot`: **24/24** لقطة مطابقة (12 nosemantic + 12
semantic).

**دقة التقدير (`scripts/dashboard-eta-check.mjs`):** 6 مهام urgent قبل
تشغيل العمّال، نقيس expected من `observe.jobPosition` عند الإدخال
ونقارنه بـactual من `QueueEvents.active`.
- الوسيط \|الخطأ\|: **3.6%** (البوابة ≤ 30% — عبرت بهامش ×8)
- المتوسط \|الخطأ\|: 2.7%
- كل التقديرات ضمن ±6% من الفعلي

**اختبار الذروة (`scripts/test-peak-load.mjs`):** 9/9 مكتملة، أقصى wait
12.10s (البوابة ≤45s). round-robin واضح. لم يتأثر بتعديلات observe.

### اللقطات (2026-09-02 ✅)

`apps/dashboard/screenshots/`:

**دوران السِيَر (6 لقطات):** `ops-queue-full`, `ops-in-flight`,
`ops-completed`, `client-in-queue`, `client-in-flight`, `client-empty`.

**دوران اللغات (6 لقطات):** `ops-ar`, `client-ar`, `ops-mixed`,
`client-mixed`, `ops-en`, `client-en`. الأوضاع الثلاثة تعمل من نفس
البنية عبر `?locale=X`.

### دروس (تُبلَّغ لتُكتب في LESSONS.md من main)

**L-dash-1 — القسمة على المتوازي، لا الضرب بها.**
كتبت `expectedStartSec = (position - 1) * avgMs / concurrency / 1000`.
مع c=2 وavg=3s: pos=2 → 1.5s المتوقعة، والفعلي 0s (لأن ثاني مهمة تبدأ
بالتوازي مع الأولى). الخطأ 100%.

*التطبيق:* حين c عمّال متوازيين، المهمة رقم p تبدأ عند
`floor((p-1)/c) * avg`. لا `(p-1)*avg/c`. الفرق: الأول يتدرج بخطوات
بحجم `c`؛ الثاني يتدرج بلا خطوة. جرِّبها ذهنياً بـp=1,2,3,4,c=2.

**L-dash-2 — BullMQ 5 يفصل «prioritized» عن «waiting». محروسة باختبار.**
كل مهامنا لها priority (الحصة العادلة)، فتدخل مجموعة "prioritized"
لا "waiting". `getState()` يعيد `'prioritized'` لا `'waiting'`،
و`getWaiting()` لا يشملها، و`getJobCounts()` يحتاج المفتاح صراحةً.

*التطبيق:* أي كود يستعمل BullMQ 5 مع priority يجب أن يجمع
`prioritized + waiting` في كل تعداد ويعالج `prioritized` كحالة انتظار.

*الحرس:* `apps/renderer/src/observe.test.ts` — 5 اختبارات تفشل إن رجع
الكود عن الجمع.

**L-dash-3 — Next.js + workspace ESM يحتاجان webpack alias يدوي.**
`import './queues.js'` من ملف TS يعمل مع tsx (moduleResolution: bundler)
لكن Next webpack لا يعرف كيف يحلّه. الحل:
```js
config.resolve.extensionAlias = { '.js': ['.ts', '.tsx', '.js'] };
```
كذلك skia-canvas (الترانزيتيف عبر renderer→worker) يجب استبعاده من
الحزمة عبر `serverComponentsExternalPackages`.

*التطبيق:* أي app جديد يستهلك حزم TypeScript ESM من workspace: أضف
هذين للـnext.config.mjs.

**L-dash-4 — الصيانة تعني إيقاف الطوابير، لا مجرد علم.**
البداية كانت: علم Redis يقرأه `systemStatus`. لكن العميل الذي يُدخل
مهمة أثناء الصيانة، تدخل الطابور وتُنفَّذ فوراً. الصيانة يجب أن تشمل
`Queue.pause()` لكل الطوابير لتمنع السحب.

*التطبيق:* حين تُصمَّم «حالة نظام»، اسأل: ما الذي يجب أن يتوقف فعلاً؟
لا تكتفِ بعلامة عرض؛ اقفل السلوك المنطقي.

**L-dash-5 — الرسائل من الخادم يجب أن تحمل مفاتيح لا لغة.**
`systemStatus()` كان يعيد `reason: 'كل شيء على ما يرام'` (نص عربي).
في وضع en يتسرّب كما هو. الحل: يعيد `reasonKey` (مفتاح i18n)
والواجهة تترجم.

*التطبيق:* أي endpoint متعدّد اللغات يجب أن يعيد **مفاتيح** أو
**رموزاً** لا نصوصاً معدّة للعرض. الترجمة على العميل. القاعدة تشمل
رسائل الخطأ ورؤوس العتاد.

*تسرّب ثانٍ لُوحظ ثم صُحّح:* رسائل API في `apps/dashboard/app/api/**`
كانت عربية («المهمة غير موجودة»، «tenantId مطلوب في query string»).
تظهر في `actionMsg` بلوحة ops حتى في وضع en. صحّحتها بتحويل رسائل
API الداخلية إلى الإنجليزية (تقنية بحتة، آمنة عبر الأوضاع). الترجمة
على العميل ما زالت الأصح لكل نص «معروض»؛ لأخطاء التقنيين الداخلية،
الإنجليزية مقبولة.

**L-dash-6 — المركّبات الرقمية تحت RTL تحتاج `dir="ltr"` صريحاً.**
`108.0 / 460.4 GB` في `<div dir="rtl">` يظهر كأنه `GB 460.4 / 108.0`.
سبب الخلل: `/` و`GB` و`.` كلها ضعيفة الاتجاه (weak-bidi) فتأخذ
اتجاه السياق (RTL) وتُقلَب.

*التطبيق:* كل مركّب رقمي فيه فاصل (/ · ,) يجب أن يُغلَّف بـ`<Ltr>`.
الأرقام المفردة لا تحتاج. للأرقام المدمَجة في نصّ عربي عبر
interpolation (مثل «— {pct}%»)، استعمل U+200E (LRM) قبل الرقم في
القالب: `"— ‎{pct}%"`. يعزل الرقم+الرمز كوحدة LTR.

*متعلّق:* الوقت مثل «12:01:23 PM» في تذييل عربي يحتاج LRM أيضاً،
وإلا يظهر PM في الجانب الخطأ.

**L-dash-7 — قاعدة اللغة المختلطة لا تُكتب من التخمين.**
كتبت أول قاعدة mixed («التقنيات إنجليزية والواجهة عربية») نظرياً
وحين اختبرها المالك قراءةً، أنتجت نصوصاً ركيكة: «مهمتك في الـ render»،
«على cores 10» (مع قلب الترتيب)، «Refresh كل 3s». الاختبار الصحيح
هو **القراءة بصوت عالٍ**، لا التصنيف النظري.

*التطبيق:* لا تسنّ قواعد لغوية من الشكل «هذا النوع من الكلمات
يبقى X»؛ بل اكتب أمثلة كاملة، اقرأها بصوت عالٍ، والقاعدة تظهر من
النمط. عند تصميم i18n لكل لوحة/تطبيق جديد، اطلب من المستخدم قراءة
لقطات فعلية قبل تعميم القاعدة.

**L-dash-8 — البوابات الكمية لا تكشف الخلل النصّي/البصري (طبقة من L-16).**
`pnpm test` مرّ · `verify:snapshot` مرّ · ETA مرّ بـ3.6%. ومع ذلك
النصّ في mixed كان مكسوراً، ورسائل API عربية تتسرّب في en. البوابات
الكمية تحرس السلوك؛ الجودة اللغوية/البصرية تحتاج قراءة يدوية.

*التطبيق (خطوة إلزامية بعد أي عمل لغوي/بصري):* اقرأ كل نص في كل
لقطة. تحقق من: لا تجاوز حاويات، لا نص مقلوب، لا تسرّب لغة الخادم،
تناظر التخطيطين LTR/RTL. البوابات الكمية شرطٌ لا كافٍ.

### دَين مقصود (يُنقل لاحقاً)

- **بيانات فوترة وسجل:** أُخفيت البطاقات في /client حتى تصل بيانات
  حقيقية (PostgreSQL — المرحلة 4).
- **`brand.locale` في brandKit:** الحفظ الحالي في localStorage فقط.
  المرحلة 4: يُخزَّن مع هوية العميل (تفصيلاً أعلاه في «الحفظ»).
- **إعادة تشغيل العامل تلقائياً من اللوحة:** يتطلب Redis pub/sub
  يستقبله worker.ts — مقفل على main. اللوحة تخزّن `WORKER_COUNT` في
  Redis كإرشاد، والتفعيل يدوي.
- **قتل مهمة نشطة:** يتطلب token من العامل. المسار الحالي: 409 مع
  رسالة توجّه لإيقاف العامل يدوياً.
- **الويب هوك مُهيَّأ لكنه غير مربوط بمصدر تنفيذ.** المستدعي يستدعي
  `runAlertCycle()` من cron/loop. لم يُضف job مجدول بعد.

### التبعيات المضافة

- `next@14.2.15`, `react@18.3.1`, `react-dom@18.3.1`
- `tailwindcss@3.4.14`, `postcss@8.4.47`, `autoprefixer@10.4.20`
- Chrome 152 مطلوب فقط لتشغيل سكربتات اللقطات؛ اللوحة نفسها تعمل في
  أي متصفح حديث.

### القيد الحاكم — الملفات المقفلة على main

لم يُلمس أيّ من:
- `apps/renderer/src/queues.ts` · `worker.ts` · `validate.ts` ·
  `index.ts` · `cli.ts`
- أيّ من `packages/**` (بما في ذلك timeline-v2 الجديدة من main)

التعديلات على الجذر: `apps/renderer/package.json` (إضافة exports)،
`vitest.config.ts` (إضافة `apps/**/*.test.ts` للـinclude)،
`.gitignore` (تجاهل `.next/` و`next-env.d.ts`). كلها إضافات
غير-تدميرية.

### التالي (يقرّره main عند الدمج)

- تشغيل `runAlertCycle` كل 30 ثانية عبر cron خارجي أو job BullMQ
  مكرر (`repeat`).
- ربط تيليجرام بالويب هوك عبر Vercel webhook أو Cloudflare Worker
  خفيف (docs/08 §المراقبة).
- بعد المرحلة 4 (PostgreSQL): تفعيل «سجل التصديرات» و«الاستهلاك».
- بعد المرحلة 4 (BrandKit موسّع): تفعيل `brand.locale`.
