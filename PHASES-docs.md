# PHASES-docs — جلسة عقد الواجهة للمرحلة 4

> فرع `docs/marketing` · جلسة موازية · **لا تعديل خارج docs/**
> PHASES-docs.md السابق أُدمج في PHASES.md عبر commit `e06d166`.
> هذا الملف يفتح جلسة جديدة.

## المهمة

كتابة `docs/16-api-contract.md` — عقد واجهة كامل للمرحلة 4، ليعمل
عليه مساران متوازيان:
- **mk-api** (البنية) — يبني ما يعِد به العقد.
- **mk-studio** (الواجهة) — يستهلك العقد قبل جاهزية البنية.

**العقد واجهة لا تنفيذ.** لا مخطط قاعدة بيانات، لا كود، لا اختراع
موارد. الغموض يُكتب كسؤال مفتوح في نهاية الملف.

## المصادر المقروءة

- `docs/02-architecture.md` §نموذج البيانات + كل ADR (بما فيها ADR-010 الجديد)
- `docs/03-brand-kit-spec.md` — بنية `brandKit` الكاملة (كل الحقول)
- `docs/13-asset-lifecycle.md` — إصدارات الأصول و `assets.version`
- `docs/14-revisions.md` — سجل المراجعات + الاستعادة الإلزامية
- `docs/15-editorial-workflow.md` — الحالات · الأدوار · التحوّلات
- `PHASES.md §المرحلة 4` — النطاق الكامل بعد قرارات 2026-09-03/04

## القيود الحاكمة على العقد

1. **RLS من أول جدول** — كل استعلام مقيّد بـ`tenant_id` على مستوى القاعدة.
   العقد يوضّح: **`tenant_id` يأتي من الجلسة (JWT claim)، لا من المسار**
   في الحالة العامة. الاستثناءات (لو وُجدت) صريحة.
2. **`brand_snapshot` في renders** — يُلتقط لحظة إنشاء المهمة (POST /renders)،
   قبل أي تعديل لاحق على brand_kit المصدر.
3. **`licenseAck` إلزامي** لرفع الخطوط + شعارات المنصات (`logoAcks`)
   + ملفات Lottie. العقد يجعل الحقل مطلوباً (400 لو غاب).
4. **المفاتيح لا تُعاد أبداً** — استجابات BYO-key تعيد `apiKeyRef`
   (مرجع) لا القيمة. المستخدم يعيد الإدخال إن أراد التغيير.
5. **الاستعادة نقطة نهاية صريحة** — `POST /:resource/:id/revisions/:revId/restore`.
   لا استنتاج من PATCH.

## حالة الجلسة

| البند | الحالة |
|---|---|
| قراءة المصادر | ✅ |
| بنية العقد جاهزة | ✅ |
| كتابة `docs/16-api-contract.md` | ◐ |
| الأسئلة المفتوحة | تُجمع أثناء الكتابة |
| فحص النطاق قبل الالتزام | معلَّق |

**الفرع:** `docs/marketing` · بلا commits جديدة بعد.
