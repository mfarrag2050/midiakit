# PHASES-docs — تدقيق تناسق الوثائق

> فرع `docs/marketing` · جلسة موازية · **لا تعديل خارج docs/**
> النتائج جدولية — القرار للمالك. لم يُصحَّح شيء.

## نطاق التدقيق

**24 ملفاً · 6,747 سطراً** فُحصت عبر خمسة مؤشِّرات متوازية ثم دُقِّقت
عيّناتها يدوياً:

- `docs/01..15` · `INVENTORY.md` · `PROJECT_INSTRUCTIONS.md`
- `M1-marketing-assets.md` · `M2-launch-collateral.md`
- `PHASES.md` · `LESSONS.md`
- `ATTRIBUTIONS.md` · `CLAUDE.md` · `README.md`

**الأدلة الميدانية المُتحقَّق منها بالأمر:**
- ملفات القوالب الفعلية (`packages/templates/src/templates/`)
- سكربتات فعلية (`scripts/`)
- مجلد demo/ ومجلدات snapshots\*
- ملف الخدمات (`services/diacritizer/`)
- ملف lottie (`fixtures/lottie/basic-shapes.json`)

---

## ملخّص تنفيذي

| # | المحور | العدد | أعلى خطورة |
|---|---|---|---|
| 1 | تناقضات بين الملفات | 14 | حالة الوثائق الجذرية (CLAUDE.md · README.md · PROJECT_INSTRUCTIONS.md) متجمّدة قبل المرحلة 3 |
| 2 | أرقام بلا مصدر | 12 | إسقاطات `docs/14` (5.5M سطر/سنة) — تخالف قاعدة CLAUDE.md الصريحة |
| 3 | حالات غير مُحدَّثة | 11 | بنود ☐ داخل مرحلة ☑ (Phase 1 و Phase 3) |
| 4 | مراجع مكسورة | 7 | `scripts/diagnose-render-perf.mjs` مذكور 4 مرات وهو محذوف |

**السؤال الحاكم:** ثلاث وثائق «إطارية» — `CLAUDE.md` · `README.md` ·
`PROJECT_INSTRUCTIONS.md` — كلها تصف مشروعاً في «المرحلة صفر». الواقع
في PHASES.md المرحلة **3.8**. من يفتح المستودع للمرة الأولى سيقرأ
وصفاً مضلِّلاً في أول ملف يفتحه.

---

## 1. تناقضات بين الملفات

| # | الملف · الموضع | المشكلة | الحكم المقترح | الأهمية |
|---|---|---|---|---|
| 1.1 | `CLAUDE.md:142` ↔ `PHASES.md:64-67` | «المرحلة صفر — لم يُكتب كود منتج بعد» في CLAUDE.md بينما PHASES يعلن 0/1/1.5/2/3/3.2/3.7 ☑ و3.5/3.8 ◐ | تحديث سطر «عند آخر تحديث» ليعكس المرحلة 3.8 مع تاريخ آخر جلسة | **عالية** |
| 1.2 | `README.md:40-50` ↔ `PHASES.md:64-67` | README مؤرّخ 2026-08-31 يعرض 0/1/1.5/2 ☑ و3 ◐ و3.5/3.7/4/5 ☐. الواقع: 3/3.2/3.7 ☑ و3.5/3.8 ◐ | إعادة توليد جدول الحالة من PHASES.md بتاريخ جديد | **عالية** |
| 1.3 | `PROJECT_INSTRUCTIONS.md:46` ↔ الواقع | «المرحلة صفر — لم يُكتب كود المنتج بعد» — الملف بأكمله مجمّد قبل بدء العمل | حذف الملف أو إعادة كتابته كملخّص إشارات لـCLAUDE.md/PHASES.md/README.md لا كمرجع منفصل | **عالية** |
| 1.4 | `PROJECT_INSTRUCTIONS.md:26-34` ↔ `CLAUDE.md:22-42` | تسع قواعد فقط بينما CLAUDE.md فيه عشر + قاعدة 9 السابقة موصوفة بأنها «نُقضت». PROJECT_INSTRUCTIONS يعرض قاعدة 9 كأنها سارية «محرّر خط زمني كامل من الإصدار الأول» | مصدر واحد للقواعد (CLAUDE.md). PROJECT_INSTRUCTIONS يشير إليه لا يكرّره | **عالية** |
| 1.5 | `CLAUDE.md:117` ↔ `docs/LESSONS.md` | CLAUDE.md يقول «ستة دروس» ثم يذكر L-03..L-07 (يوحي بسبعة إجمالاً). LESSONS.md يحمل فعلياً **36 درساً** (L-01..L-36 + L-40) | تحديث نصّ CLAUDE.md وحذف الإشارة إلى عدد. الاكتفاء بـ«اقرأ docs/LESSONS.md كاملاً» | **عالية** |
| 1.6 | `README.md:64` ↔ `docs/LESSONS.md` | «6 دروس في LESSONS.md» — بينما الفعلي 36 | تحديث العدد أو حذف الرقم | متوسطة |
| 1.7 | `README.md:47,55` ↔ `M1:172,180` ↔ الفعلي | README: «6 قوالب · 12 لقطة ذهبية · 179/179 اختبار · 12/12 verify:snapshot». الفعلي (تحقّقت `ls`): **12 لقطة في `snapshots/` + 12 في `snapshots-semantic/` = 24**، وM1 يذكر «238–266 اختبار» عبر عدة جلسات | تحديث README بأرقام PHASES.md اليوم | متوسطة |
| 1.8 | `docs/09:52` ↔ `docs/06:29,56` ↔ الواقع | 09 يقول «أربعة قوالب تكفي، لا خامس قبل العميل الرابع». 06:29 يقول «12 لقطة = 6 قوالب × 2 هويتين» و06:56 يعدد خمسة أسماء تحت «القوالب الأربعة». الفعلي: **ستة قوالب** (breaking · card-centered · card-bottom · card-kicker · reel · plain) | إمّا: (أ) توحيد اللغة على «ست قوالب» في كل مكان، أو (ب) حصر البوابة التسويقية في 09 على «أربعة تكفي» مع إقرار صريح أن plain و reel قوالب بنية لا قوالب عميل | **عالية** |
| 1.9 | `docs/04:46` ↔ داخل نفس الملف | يقول «تحويل القوالب الأربعة» ثم يعدّد خمسة (card_centered · card_bottom · card_kicker · breaking · reel) | تعديل العنوان إلى «تحويل القوالب الخمسة الموروثة» (plain لاحق) | متوسطة |
| 1.10 | `docs/06:98,135` و `docs/06:108,145` ↔ داخل الملف | المرحلة 3.7 والمرحلة 4 مذكورتان مرتين كاملتَين — قوائم مهام مكرّرة حرفياً | حذف التكرارين — 3.7 لم تعد قائمة بذاتها (الواجهة انتقلت إلى 4) | متوسطة |
| 1.11 | `docs/02:52` ↔ `docs/03:85` ↔ `docs/04:41` | shortRatio/shortLineRatio: 02 يذكر «0.6» كوصف لـwrapAlternating؛ 03 يجعل الافتراضي 1.0 (uniform)؛ 04 يفرض 0.6 لـalternating. القيمة معلَّقة على mode لكن الملفات لا تُظهر الشرط بوضوح | إضافة فقرة إيضاح في 03 تربط الحقل بـmode، وتحديث 02 ليقول «uniform 1.0 · alternating 0.6» | متوسطة |
| 1.12 | `docs/06:79` ↔ `docs/08:152-154` | 06: «تسع مهام متزامنة ⇒ ≤45 ثانية للبدء». 08:152 يقول «≤60 ثانية»، و 08:154 يقول «≤ دقيقتين ذروة» | حسم رقم واحد (**15.13s المُقاس** في PHASES.md:306) وتوحيد الالتزام في 06 و 08 و CLAUDE.md | متوسطة |
| 1.13 | `docs/12:59,85` ↔ `PHASES.md:669` ↔ MEMORY | 12 يقول «طبقة الإسناد للإصدار الأول» بينما PHASES.md:669 يحملها كمُنجَزة (2026-09-02) في 3.8 | تعديل 12 لتُصنَّف كـ«شُحنت في 3.8» بدل «للإصدار الأول» | متوسطة |
| 1.14 | `PHASES.md:306` (15.13s) ↔ `PHASES.md:401` (12.10s) ↔ `M1:188` (12.10s) | رقمان مختلفان لـ«9 مهام متزامنة max wait» — 15.13s من اختبار المرحلة 3 و12.10s من بوابة المرحلة 3.2. M1 يستخدم 12.10s كرقم واحد | تسمية كل رقم بمصدره في M1 والوثائق، أو إعادة قياس واحد نهائي | منخفضة |

---

## 2. أرقام بلا مصدر قياس

قاعدة CLAUDE.md § «سجل الأصول التسويقية»: «رقم بلا مصدر قياس لا يُكتب.»

| # | الملف · الموضع | الرقم | ما ينقص | الحكم المقترح | الأهمية |
|---|---|---|---|---|---|
| 2.1 | `docs/14:84` | 30 حقلاً × 5 تعديلات × 100 عميل × 365 يوماً = **5.5 مليون سطر/سنة** | لا سطر قياس في PHASES.md ولا سكربت — إسقاط تخميني | إما (أ) نسبه صريحاً كـ«تقدير مبدئي — يُعاد قياسه عند عميل أول»، أو (ب) حذفه | **عالية** |
| 2.2 | `docs/13:161` | «5 من 6 منصات ترفض إعلان التغيير» | قائمة المنصات موجودة في ATTRIBUTIONS، لكن الرقم مطلق بلا رابط لعدّها | إضافة «حسب جدول ATTRIBUTIONS §شعارات المنصات» أو إعادة صياغة | متوسطة |
| 2.3 | `docs/02:97` | «~6GB مؤقتة/فيديو» | تُكرَّر في `08:79` بلا مصدر | نسبه إلى `docs/10 §ذات القرص` (المصدر التقديري الأصلي) أو قياسه | متوسطة |
| 2.4 | `docs/02:100` | «500GB قرص edit» | قرار تقديري، ذكره في PHASES.md غير موثّق كسطر قياس | نسبه إلى قرار docs/10 لا كواقع | منخفضة |
| 2.5 | `docs/08:94` | «مشروع تحرير يشغل عاملاً 7 دقائق» | لا سكربت ولا سجل — تقدير | إضافة «تقديري — قياس فعلي في المرحلة 4 بعد أول عميل edit» | متوسطة |
| 2.6 | `docs/08:158` | «60 مهمة/ساعة · 70% استغلال» | بلا سكربت في scripts/ | نسبه أو تقييمه كتقدير | متوسطة |
| 2.7 | `docs/08:172` | «40-120ms و ~2.5:1» | بلا مصدر — الأداء الحالي بحسب اختبارات PHASES.md مختلف | مواءمة الرقم مع PHASES.md:289 (2.2s على 8.4s = 0.26:1) | متوسطة |
| 2.8 | `docs/10:180` | «فيديو 3 دقائق ~7 دقائق رندر» | بلا مصدر — تقدير أوّلي | نسبه صريحاً كتقدير | منخفضة |
| 2.9 | `docs/12:98` | «~2MB نموذج كشف الوجوه الخفيف» | بلا اسم نموذج ولا قياس | تعليقه «حين يُختار النموذج، يُقاس ويُوثَّق» | منخفضة |
| 2.10 | `docs/12:139` | «150-300$ شهرياً لخادم GPU» | بلا مصدر تسعير | نسبه إلى موفّر (Vast/RunPod/…) أو حذفه | منخفضة |
| 2.11 | `docs/01:88` | «49$ Bannerbear / 19$ Placid / 41$ Creatomate» | تاريخ الرصد غير مذكور | إضافة «رُصد YYYY-MM-DD — أسعار حية قابلة للتغيّر» | منخفضة |
| 2.12 | `docs/07:41,50` | «WebKit #6203, Mozilla #185600 · 20 عاماً» | أرقام بلاغات — قابلة للتحقق بروابط لكن الروابط غائبة | إضافة روابط bugzilla/webkit لتثبيت المصدر | منخفضة |

---

## 3. حالات غير مُحدَّثة

قارنّا مسنّنات (`☑/◐/☐/⏸/📎`) في PHASES.md مع بقية الوثائق ومع بعضها.

| # | الملف · الموضع | الحالة المسجَّلة | الحالة الفعلية | الحكم المقترح | الأهمية |
|---|---|---|---|---|---|
| 3.1 | `PHASES.md:157` | ☐ ربط `preprocessBidi` بالواجهة الحالية | المرحلة 1 ☑ عموماً · البند مؤجَّل صراحةً إلى 2/4 في السطر 203 نفسه | تحويل ☐ إلى 📎 (مؤجَّل بقرار موثّق) | متوسطة |
| 3.2 | `PHASES.md:178-182` | 5 بنود ☐ (mediakit-assets · 7 خطوط · اختبار الكشيدة لكل خط · catalog.json · audio فارغ) | المرحلة 1 ☑ لكن هذه ☐ | (أ) نقل هذه إلى المرحلة 4 صراحةً، أو (ب) إغلاقها إن كانت شُحنت في مكان آخر (audio فارغ صحيح بيّن) | **عالية** |
| 3.3 | `PHASES.md:190` | ☐ `detectFontCaps` عند رفع الخط | المرحلة 1 ☑ · كود `detectFontCaps` قائم في engine (`kashida.ts`) لكنه لا يُستدعى وقت الرفع | فصل «الكود موجود ☑» عن «التكامل مع الرفع 📎 مؤجَّل لواجهة المرحلة 4» | متوسطة |
| 3.4 | `PHASES.md:192` | ☐ تفكيك `cvRenderInto` إلى مفسّر طبقات | المرحلة 2 ☑ و `renderFrame` هو المفسّر (السطر 267) | تحديث ☐ إلى ☑ مع الإشارة إلى renderFrame | **عالية** |
| 3.5 | `PHASES.md:199,200` | watermark ☐ · headline/kicker/source ☐ | السطر 251 يعلن watermark ☑ (دَين مُحسَم)، والسطر 249 يعلن kicker ☑ | تحديث السطرين — كلاهما مغلق | **عالية** |
| 3.6 | `PHASES.md:277-279` | 3 بنود ☐ (`apps/renderer` · `render:png` · مقارنة بكسلية) | المرحلة 3 ☑ · `apps/renderer/src/index.ts` قائم (السطر 286) · `render:mp4` يعمل (السطر 289) | تحديث ☑ للأول والثاني · الثالث يبقى 📎 (مع الواجهة) | **عالية** |
| 3.7 | `PHASES.md:316-317` | ☐ WebCodecs · ☐ الخادم كتراجع | البند بطبيعته مؤجَّل مع الواجهة | إعادة تصنيفهما 📎 لا ☐ | منخفضة |
| 3.8 | `PHASES.md:65-67` (سطر الحالة) | يُلخِّص 3.5 ◐ و 3.7 ☑ لكن **يُغفل 3.8** | 3.8 ◐ حسب السطر 101 | إضافة «3.8 ◐» إلى السطر الملخّص | متوسطة |
| 3.9 | `INVENTORY.md:151-170` | 6 بنود «تُحذف على main» + «القيم اللونية تُترك حتى المرحلة 1» | المرحلتان 0 و 1 ☑ منذ فترة — الحذف تمّ · القيم اللونية أُغلقت | إمّا تحويل INVENTORY إلى ملف تاريخي (Prefix ARCHIVE) أو تحديث نصّه ليقول «تمّ 2026-XX-XX» | متوسطة |
| 3.10 | `docs/12:69,85` | Lottie: «مؤجَّل 2026-09-02» + بند تحضيري «منجَز» (`fixtures/lottie/basic-shapes.json`) | الفعلي: الملف موجود ☑؛ Lottie نفسه مؤجَّل ☑ — متسق داخلياً لكن ATTRIBUTIONS:293-307 يحتاج مواءمة مع «licenseAck placeholder 2026-09-XX» | استبدال placeholder بتاريخ فعلي أو حذف الحقل حتى يُقرَّر | منخفضة |
| 3.11 | `docs/13:141-150` و `docs/14:133-140` و `docs/15:200-208` | كل ملف يعلن «بنود جديدة في PHASES.md §المرحلة 4» | PHASES.md:785-815 يحمل البنود فعلاً — متسق | لا إجراء (تحقق سلبي) | منخفضة |

---

## 4. مراجع مكسورة

| # | الملف · الموضع | المرجع | حالة الوجود | الحكم المقترح | الأهمية |
|---|---|---|---|---|---|
| 4.1 | `PHASES.md:978` (سجل القرارات) · `LESSONS.md:121,136` · `M1:164` | `scripts/diagnose-render-perf.mjs` | **محذوف صراحةً** حسب `PHASES.md:636` («حُذف diagnose-render-perf.mjs و diagnose-semantic-perf.mjs») — بحث `ls scripts/` يؤكّد الغياب | (أ) في PHASES.md وLESSONS.md: إبقاء الإشارة كسجل تاريخي مع تعليق «(محذوف بعد الإنجاز، السجل في L-07)». (ب) في M1:164 استبدال «الدليل: السكربت» بـ«الدليل: سطر PHASES.md:978 وM1 §مرجع الأداء» | **عالية** |
| 4.2 | `LESSONS.md:136` | «`scripts/diagnose-render-perf.mjs` **باقٍ**» | التصريح خاطئ (محذوف) | تعديل الجملة إلى «قِيس على نفس المنهج—» مع إزالة الادعاء بأن السكربت باقٍ | **عالية** |
| 4.3 | `M1:73,284` | `scripts/eval-semantic-break-rss.mjs` | **غير موجود** — الفعلي `scripts/eval-semantic-rss.mjs` (المذكور بشكل صحيح في M1:59) | توحيد اسم السكربت في M1 (يوجد كذلك `eval-semantic-break-internal.mjs` منفصل) | **عالية** |
| 4.4 | `PHASES.md:725` و `docs/12:130` | `L-13` (أو «L-13-مماثل») | **غير موجود في LESSONS.md** — التسلسل يقفز من L-12 إلى L-14 (تحقّقت بـgrep) | (أ) كتابة L-13 لسدّ الفجوة (قد يخصّ «كل مخرَج نموذج اقتراح قابل للتحرير»)، أو (ب) تحديث الإشارتين لتلائما درساً موجوداً | **عالية** |
| 4.5 | `docs/15:141,231` | `L-32 المرتقب` | **موجود فعلياً في `LESSONS.md:917`** — العبارة «المرتقب» خاطئة | حذف «المرتقب» — الدرس كُتب | متوسطة |
| 4.6 | `INVENTORY.md:3` | «نسخة `main` عند الالتزام `ee178ca`» | الالتزام موجود لكن هو **قبل** تنظيف المرحلة 0. `git log` يُظهر تنظيف الأصول جاء لاحقاً (`de03a99`) | تحديث المرجع إلى `de03a99` أو تصريح «حالة الأداة قبل تنظيف المرحلة 0» | متوسطة |
| 4.7 | `docs/15:154` | «المرحلة 4.5 — Slack/WhatsApp webhook» | لا وجود لمرحلة 4.5 في PHASES.md ولا CLAUDE.md | إمّا (أ) إضافة 4.5 في PHASES.md كمرحلة نمو، أو (ب) نقل البند إلى المرحلة 5 وإعادة صياغة | منخفضة |

---

## 5. ملاحظات جانبية (لا ترقى إلى تناقض)

- **`docs/07:39,41,50`** — أرقام bugzilla قابلة للتحقق ولكن الروابط
  الكاملة غائبة. إضافة رابط واحد كافية.
- **`docs/09:187`** — جدول جدول زمني (2+4+3+3=12 أسبوعاً) لا يوافق مجموع
  06 (>20 أسبوعاً). 09 يعرض نطاقاً تسويقياً و06 خطة داخلية — يستحق تعليق
  إيضاحي في 09.
- **قوائم الملفات المقفلة في `docs/11:58-70`** لا تذكر ملفات المرحلة 3.8
  المُضافة (attribution/simple-icons). ليست مشكلة اليوم لأن العمل الحالي
  توثيقي — لكن قبل فرع فرعي جديد يلمس packages، ستحتاج التحديث.
- **معظم مسارات `out/*.png` المذكورة في M1 وATTRIBUTIONS** لا تُخزَّن في
  git (المجلد غير موجود). هي مخرجات سكربتات تُولَّد وقت الطلب — منطقي،
  لكن يستحق سطراً في M1 يوضّح «تُولَّد بأمر X، ليست committed».

---

## 6. توصية ترتيب المعالجة

المالك يقرّر — لكن ترتيب طبيعي (من الأثر إلى الجهد):

1. **الحالات الجذرية أولاً** (`CLAUDE.md:142` · `README.md` كامل ·
   `PROJECT_INSTRUCTIONS.md` كامل) — كل قارئ جديد يقع عليها أولاً.
2. **سدّ فجوة L-13** — إشارة معماريّة متكرّرة (بندان: PHASES.md:725
   و docs/12:130) بلا درس مُلحَق.
3. **تصحيح مسنّنات PHASES.md** (المجموعات 3.4 · 3.5 · 3.6) — 30 دقيقة عمل
   وتُطهّر الملف الحاكم للحالة.
4. **تصحيح أسماء السكربتات في M1** (المجموعتان 4.1 · 4.3) — قبل أي
   استعمال تسويقي للأرقام.
5. **إسقاطات docs/14** (المجموعة 2.1) — بند واحد لكنه سنّ مخالف صريح
   لقاعدة CLAUDE.md.
6. الباقي دفعات عند الاستراحات — لا يحجب عملاً.

---

**الجلسة:** فرع `docs/marketing` · مجلد `pf-mediakit-docs` · لا تعديل
خارج docs/ · التقرير جدولي، لا تصحيحات في هذه الجلسة.

---

## سجل التنفيذ (2026-09-03)

بعد التقرير، أذن المالك بتصحيحات على دفعتين. **22 بنداً عولجت · 21 تُركت
عمداً · 1 تحقّق سلبي.** التفصيل في تسليم الجلسة.

**بنود تُركت للمالك بعد الجلسة** (بحاجة قرار أو حسم تسويقي):

| # | الوصف الموجز |
|---|---|
| 1.4 | تسع قواعد في `PROJECT_INSTRUCTIONS.md` مقابل عشر في `CLAUDE.md` |
| 1.5 | عدّ الدروس في `CLAUDE.md:117` (المذكور 6، الفعلي 36) |
| 1.8/1.9 | «أربعة قوالب» (09) مقابل «ست قوالب» (06/07/الفعلي) — قرار تسويقي |
| 1.10 | تكرارات المرحلتين 3.7 و 4 في `docs/06` — تنظيف ميكانيكي |
| 1.11 | إيضاح `shortLineRatio` بحسب mode في 02/03/04 |
| 1.12 | SLA عاجل: 45s / 60s / دقيقتان — يحتاج قياساً أو حسماً |
| 1.13 | `docs/12:59,85` «الإسناد للإصدار الأول» — يعدَّل إلى «شُحن في 3.8» |
| 1.14 | 15.13s ↔ 12.10s في PHASES/M1 — تسمية كل رقم بمصدره |
| 2.2 → 2.12 | أحد عشر رقماً بلا مصدر (تقديرات · أسعار منافسين · أرقام bugzilla · تكاليف مزوّدين) — تصنيف بالجملة |
| 3.10 | placeholder `2026-09-XX` في ATTRIBUTIONS للـLottie licenseAck |

**بنود حُسمت بقرار المالك بعد التقرير:**

| البند | القرار |
|---|---|
| علامة النسخة التجريبية (SAMPLE_ONLY) | **مؤجَّلة إلى المرحلة 4 تحت الاشتراكات** — سُجّلت في `PHASES.md §المرحلة 4 §الأعمدة الأساسية`. المرجع القديم في `docs/03` يبقى حتى تُحسم البنية |

---

## حالة الدمج

**جاهز للدمج، بانتظار إشارة المالك — تعارض متوقّع في `PHASES.md` مع جلسة `main`.**

- `git rebase origin/main` = **no-op** — `origin/main` عند `03a25d0`، لم يتقدَّم أثناء الجلسة.
- **فحص النطاق:** `git diff origin/main --stat -- packages apps scripts snapshots snapshots-semantic` = **فارغ**. لا لمس خارج docs/ + الجذور المأذون بها.
- **الاختبارات:**
  - `pnpm check:engine-purity` ✅ نظيف.
  - `pnpm test` (vitest) و `pnpm verify:snapshot` ⏸ متعذّران — `node_modules` غير مثبَّت في هذا الوركتري. ينتظران قرار `pnpm install --frozen-lockfile`.
- **السبب في الانتظار:** جلسة `main` تعمل على طبقة الترجمة (caption) وتعدّل `PHASES.md` بالتوازي — الدمج الآن سيتعارض. المالك يطلب الدمج عند نقطة توقّف في main.

**الفرع:** `docs/marketing` · **الالتزام:** `afe1caf` + تعديلات هذه الجلسة (غير ملتزَمة بعد).

**تنبيه على تقدّم main:** أثناء كتابة هذا التقرير، تقدَّم `origin/main`
بثلاث commits: `12cf419` (خدمة التفريغ) · `76fb2aa` (طبقة `caption` في
المحرك) · `108baef` (استقرار الكشيدة داخل caption). البنود الثلاثة قد
تكون غيّرت حالة 3.8 على main. سطر الحالة الذي أدخلتُه (التفريغ ◐) مبنيّ
على رسالتك السابقة — يحتاج إعادة قراءة بعد الدمج.

---

# فجوات التوثيق (2026-09-03)

> فحص الاكتمال لا التناسق. أربعة محاور: قرارات بلا توثيق · مواصفة
> مقابل كود · دروس بلا تطبيق · مؤجَّلات بلا شرط إعادة فتح. لم يُصحَّح
> شيء — التصنيف للقرار.

## ملخّص تنفيذي

| المحور | العدد المُشتبَه | أبرز فجوة |
|---|---|---|
| 5. قرارات بلا توثيق | 22 | شجرة `docs/02-architecture.md` قديمة — لا تعرف `services/` ولا `apps/dashboard` ولا `brands/` ولا `data/external/` |
| 6. مواصفة ↔ كود (brandKit + templates) | 30+ | جدول أنواع الطبقات في `docs/04` قديم كلياً؛ طبقة `attribution` غائبة عنه؛ حقول موصوفة في docs/03 بأسماء لا تطابق الكود (`fonts.source`, `catalogId`) |
| 7. دروس بلا تطبيق | 9 من 38 | L-33 خصوصاً (بوابة توقّف مثل L-32 لكن غير مسجَّلة في §بوابات التوقّف بـCLAUDE.md) |
| 8. مؤجَّلات بلا شرط إعادة فتح | 21 من 39 | 6 بنود بلا سبب **ولا** شرط · 9 بنود بشرط ضبابي («المرحلة 4»، «حتى أول عميل بشعار») |

**المحور الأكثر إلحاحاً:** المواصفة ↔ الكود. مطوّر جديد يقرأ `docs/03`
أو `docs/04` سيبني توقعات معطوبة عن الحقول والطبقات ــ ثم يصطدم
بالكود. باقي المحاور بطيئة الأثر؛ هذا سريعه.

---

## 5. قرارات معمارية اتُّخذت بلا سطر توثيق مقابل

| # | القرار | مكانه في الكود | التوثيق المتوقَّع | الحالة |
|---|---|---|---|---|
| 5.1 | مجلد جذر `services/` (بايثون معزول: diacritizer + transcriber) كطبقة موازية لـ`apps/` و `packages/` | جذر · commits `9516d1e`, `12cf419` | `docs/02 §البنية` (شجرة الملفات) | **مفقود** — الشجرة تذكر `engine · templates · shared · studio · renderer · api` فقط. L-12 يذكر المبدأ لا الحد المكاني |
| 5.2 | `apps/dashboard/` كتطبيق Next.js منفصل (لا داخل studio) | commits `55c2647` · `bf7e045` | `docs/02 §البنية` + سجل قرارات | **مفقود** — دمج/فصل studio/dashboard قرار معماري بلا سطر |
| 5.3 | مجلد جذر `brands/` (`client-demo.json` — الهويات في التطوير) | commit `3c4578e` | `docs/02 §البنية` | **مفقود** |
| 5.4 | مجلد جذر `data/external/` (بيانات معجمية وقت البناء) | commit `79dd8af` | `docs/02` أو `docs/03` | **مفقود** — البدائل المرفوضة (خدمة معجمية، `packages/engine/data/`) غير مذكورة |
| 5.5 | طبقة `caption` (`packages/engine/src/layers/caption.ts`) | `main` — commits `76fb2aa` + `108baef` (أثناء الجلسة) | PHASES §3.8 + docs/04 | **حاد** — الملف مدموج على main بينما PHASES.md (حتى في فرعي المعتمد على 03a25d0) يقول «لم تُبنَ». يستحق مراجعة بعد الدمج |
| 5.6 | `brand.placement.<element>` كطبقة موحّدة تفصل «أين» (الهوية) عن «أيّ» (القالب) | `packages/shared/src/brand-kit.ts:345-378` | `docs/03` (منشأ الحقل) + سجل قرارات | **جزئي** — docs/04 يوثّق قاعدة الأولوية، docs/03 لا يذكر البنية أصلاً. البديل المرفوض («لماذا هنا لا في `layer.anchor`؟») غائب |
| 5.7 | `timeline-v2/` بجانب `timeline/` القديم قبل حذف الأخير | commit `1441c66` (1519 سطراً) | سجل قرارات | **جزئي** — استراتيجية «bombardment ثم استبدال» بلا سطر يقارنها مع الاستبدال الموضعي |
| 5.8 | ثلاث حالات `logoMode: 'none'\|'generic'\|'official'` (لا `off/on` + boolean) | `packages/shared/src/brand-kit.ts:414` | سجل قرارات | **جزئي** — البديل الثنائي المرفوض غير مطروح. L-28 يغطي الجذر القانوني لا اختيار الـAPI |
| 5.9 | أيقونة `external-link` تحديداً كتصميم `generic` (سهم خارج إطار) | `packages/engine/src/layers/attribution.ts:247` | `docs/03 §attribution` | **مفقود** — glyph بعينه اختير من بين شكلين محايدين ممكنَين، بلا سطر |
| 5.10 | استعمال `LRI (U+2066) + PDI (U+2069)` بدل `LRM (U+200E)` للعزل | `packages/engine/src/layers/attribution.ts:126-135` | ADR جديد أو سجل قرارات (طباعي) | **جزئي** — التبرير مكتوب كتعليق داخل الكود («LRM يعالج حرفاً واحداً؛ LRI/PDI يعزل كامل») — يستحق ترقية إلى ADR |
| 5.11 | `AudioPlan` قيمة JSON قابلة للتسلسل، منفصلة عن `drawTimelineAt` | `packages/engine/src/timeline/audio-graph.ts` · commit `ae0db58` | سجل قرارات + `docs/05` | **جزئي** — نفس النمط: التبرير في تعليق كوديّ، يستحق ترقية |
| 5.12 | مصادر `synth-sine` · `synth-noise` داخل `AudioSource` (اختبار مُسرَّب إلى نموذج إنتاجي) | `packages/engine/src/timeline/audio-graph.ts:33-36` | `docs/10` | **مفقود** — البديل (fixture خارجي) غير مذكور |
| 5.13 | ثمانية دوال easing — `spring` و `step` مضافتان مقابل حذف Quad/Cubic | `packages/engine/src/timeline/easing.ts` مقابل PHASES:285 | سجل قرارات | **تناقض** — PHASES يوصف مجموعة قديمة (`Quad{In,Out,InOut}` + `Cubic{In,Out,InOut}` + easeOutBack) والكود يحمل مجموعة مختلفة (`linear + easeIn* + easeOut* + easeInOut* + spring + step`). التحوّل غير موثَّق |
| 5.14 | خمس انتقالات (crossfade · slide · wipe · zoom · blurIn) — لماذا هذه لا غيرها؟ | `packages/engine/src/timeline-v2/transitions.ts` | `docs/10 §المؤثرات` | **جزئي** |
| 5.15 | `kenBurns` بتسع نقاط أصل تحديداً (بدل 5 الشائع أو نموذج متصل) | commit `de9d2aa` | `docs/10` | **مفقود** — العدد 9 يظهر كواقعة، لا كقرار API |
| 5.16 | `typewriter` نص RTL حرفاً بحرف كنمط مستقل | commit `6844eea` | `docs/10` | **جزئي** — مذكور بالاسم فقط. تنفيذ RTL حرف-بحرف قرار طباعي غير تقليدي، بلا L-درس مرفق |
| 5.17 | فحص التصادم داخل `buildTimelinePlan` (لا داخل `drawTimelineAt`) | commit `1e41579` | سجل قرارات | **جزئي** — L-16 يوثّق الدرس لا القرار الموضعي |
| 5.18 | `brand.locale: string` مفتوح في الكود بينما وثائق تعامله كـ`'ar'\|'mixed'\|'en'` union محصور | `packages/shared/src/brand-kit.ts:454` مقابل `PHASES:384, 417, 766` | `docs/03` + سجل قرارات | **تناقض** — دَين مضمر بلا سطر يعترف به |
| 5.19 | تسع سكربتات `verify-*.mjs` مستقلة كنمط بوّابات موحَّد | `scripts/` | `docs/06` أو `docs/11` | **مفقود** — البديل (vitest suite أو CI job واحد) غير مطروح |
| 5.20 | خمسة مجلدات لقطات في الجذر (`snapshots/` · `snapshots-semantic/` · `snapshots-pre-placement/` · `snapshots-semantic-pre-placement/` · `snapshots-video/`) | جذر | `docs/11` أو سجل قرارات | **جزئي** — النمط «حفظ الإصدار قبل قرار بصري كبير كـ`snapshots-pre-<X>/`» استقرَّ بلا اسم |
| 5.21 | `AttributionLogoAck.ackBy + ackAt` (سجل قانوني داخل نموذج بيانات إنتاجي) | `packages/shared/src/brand-kit.ts:419-429` | `docs/03` + سجل قرارات | **جزئي** — قرار امتثال (بدل جدول audit منفصل في المرحلة 4) بلا سطر |
| 5.22 | `PlatformKey` = 6 منصات بعينها (tiktok · x · instagram · youtube · telegram · facebook) — بلا Snapchat/Threads/LinkedIn | `packages/shared/src/brand-kit.ts:400` | سجل قرارات (منتج/قانوني) | **جزئي** — لماذا هذه في السوق الخليجي بلا Snapchat؟ غير مذكور |

**نمط متكرر:** توسّع بنية المستودع (5.1–5.4، 5.19، 5.20) دون تحديث
`docs/02 §البنية`. القاعدة الضمنية «كل مجلد جذر قرار معماري» غير محرَّرة.

---

## 6. مواصفة ↔ كود

### 6-أ. brandKit (`packages/shared/src/brand-kit.ts` ↔ `docs/03`)

**حقول في الكود بلا نظير كامل في docs/03:**

| # | الحقل | ملاحظة |
|---|---|---|
| 6.1 | `BrandPlacement` كامل (`placement.<element>` مع 9-lattice + offset) | يوجد في الكود ويُشرح في docs/04، لكن `docs/03` لا يعرّف بنية `PlacementSpec` رغم أنه حقل في `BrandKit`. المطوّر يقرأ الكود ليفهم |
| 6.2 | وراثة `title3l` و `reelTitle` من `breaking` (`minLines/preferredLines`) | مذكورة كتعليق JSONC عام؛ الدلالة (الوراثة) غير موصوفة صراحةً |

**حقول موصوفة في docs/03 بلا نظير في الكود (وعود ميتة أو تناقضات):**

| # | الحقل | مكان الوصف | التناقض |
|---|---|---|---|
| 6.3 | `brand.fonts.primary.source = catalog\|upload\|system` | docs/03 §fonts | الكود: `FontSource = 'custom' \| 'builtin'`. **قيم مختلفة** |
| 6.4 | `brand.fonts.primary.catalogId` | docs/03 §fonts | لا حقل في الكود |
| 6.5 | `brand.typography.breaking.swapMaxFsDiff` | docs/03 سطر 93 | لا نظير في `TypographyBreaking` |
| 6.6 | `brand.typography.breaking.swapMinFillGain` | docs/03 سطر 94 | لا نظير |
| 6.7 | `brand.typography.accentBar.descenderClearance` | docs/03 سطر 110 | لا نظير |
| 6.8 | `brand.typography.lineHeightMode = "dynamic"` كافتراضي | docs/03 سطر 113 | `DEFAULT_BRAND` يجعله `'fixed'` — انحراف عمدي بلا سطر يعترف به في docs/03 |
| 6.9 | `brand.typography.bidi.numerals = "arabic"` كافتراضي | docs/03 سطر 137 | `DEFAULT_BRAND` = `'latin'` |
| 6.10 | `brand.typography.semanticBreaks.useModel = "onAmbiguity"` كافتراضي | docs/03 سطر 127 | `DEFAULT_BRAND` = `'never'` |
| 6.11 | `brand.audio[].source = "upload"` | docs/03 §audio | لا حقل `source` في `BrandAudioTrack` |
| 6.12 | `brand.lottieAssets[]` (كامل) | docs/03 §lottieAssets | لا واجهة `BrandLottieAsset` — ميزة موعودة (متسقة مع تأجيل Lottie، لكن الحقل يستحق «⏸» صريحاً) |

### 6-ب. Templates (`packages/templates/*` ↔ `docs/04`)

**جدول أنواع الطبقات في `docs/04` (السطور 25-36) قديم بشكل حاد.** كل
الحقول التالية مذكورة كخصائص طبقة، بينما الكود يقرأها من `brand.*`:

| # | ما يقوله الجدول | الحقيقة في الكود |
|---|---|---|
| 6.13 | `image` — `fit, crop, fallback` | `crop` غير مطبَّق |
| 6.14 | `headline` — `offset, maxLines` | `offset` غير موجود؛ `maxLines` من `brand.typography.<font>.maxLines` |
| 6.15 | `source` — `size, anchor` | `size` غير موجود؛ من `font: brand.typography.source.size` |
| 6.16 | `badge` — `label, fill, anchor, radius` | `label`, `fill`, `radius` غير موجودة على `BadgeLayer` — من `brand.badges.<use>` |

**طبقات/حقول في الكود بلا وصف في docs/04:**

| # | العنصر | ملاحظة |
|---|---|---|
| 6.17 | نوع الطبقة `attribution` كامل | جدول الطبقات لا يذكرها. حقول `platform · mode · handleField · nameField · prefixLabel · logoModeOverride · margin · anchor` بلا وصف |
| 6.18 | `HeadlineLayer.wrap = "uniform"` | القيمة المستعملة في **5 من 6 قوالب**، بينما §أنماط اللف يذكر `balanced/alternating/manual` فقط |
| 6.19 | `HeadlineLayer.anchor` قيم `centerLower · below-kicker · middle` | الجدول يذكر anchor مجرّداً بلا تعداد القيم |
| 6.20 | `HeadlineLayer.verticalAnchor` · `HeadlineLayer.justify` | غير مذكورَين |
| 6.21 | `KickerLayer.verticalAnchor` · `KickerLayer.align` | غير موصوفَين |
| 6.22 | `AccentLayer.mode = "above-first-line"` | القيمة الثالثة غير موثّقة (يذكر `span\|underline` فقط) |
| 6.23 | `TemplateVideo.easing` (ثمانية قيم) · `.outro` · `.animation[]` تفاصيل | مذكورة في مثال breaking فقط، لا وصف نصّي عام |
| 6.24 | `TemplateField.type` قيم `medialist · range · text · richtext · image` | لا وصف نصّي عام |

**طبقات/حقول موصوفة في docs/04 بلا تنفيذ:**

| # | العنصر | التنفيذ |
|---|---|---|
| 6.25 | نمط اللف `manual` (يحترم `\n`) | لا نظير في `WrapMode` |
| 6.26 | نوع الطبقة `clipstream` (`crossfade: brand.motion.reelCrossfade`) | لا نظير في `LAYER_TYPES`. **موعودة لم تُبنَ** — reel.json الفعلي يستعمل `solid` بديلاً |
| 6.27 | `GradientLayer.direction` كـobject `{ square, portrait }` | الكود يقبل string واحد فقط |
| 6.28 | `HeadlineLayer.position/offset/shadow/anchor as object` | لا حقول بهذه الأسماء |
| 6.29 | `Template.modes` (مثل `{cover: "إطار ثابت"}` في reel) | لا حقل في `Template` |
| 6.30 | `TemplateVideo.segments` (`{min, max, duration}`) | لا حقل في `TemplateVideo` — **ميزة موعودة**. الحقل الوحيد المسموح: animation/outro/easing |
| 6.31 | `"kind": "static\|video"` كسلسلة (مثال docs/04:110) | schema.ts يمنع — يقبل `static` أو `video` فقط. **المثال خاطئ** |
| 6.32 | `HeadlineLayer.anchor: "bottom-right"` (مثال breaking) | القيمة غير مسموحة في `HeadlineAnchor` |
| 6.33 | `field key = "textScale"` (مثال breaking) | القالب الفعلي `breaking.json` لا يحمل هذا الحقل |
| 6.34 | `AccentLayer.fallback` كسلسلة (مثال card_centered) | `fallback` في الكود array من طبقات لا string |

**التقدير:** docs/04 يمثّل «الوثيقة الأكثر انفصالاً عن الكود» في المشروع.
جدول الطبقات يعود إلى قبل تحوّل نموذج «الهوية أين، القالب أيّها».

---

## 7. دروس بلا تطبيق

من 38 درساً في `docs/LESSONS.md`، **9 دروس** لا تظهر في CLAUDE.md ولا
في سجل قرارات PHASES.md ولا كبوابة/سكربت.

| # | الدرس | العنوان الموجز | لماذا هي فجوة |
|---|---|---|---|
| 7.1 | **L-15** | تغيير قرار متأخر يوجب مراجعة ما سبقه | كاشف نظري بلا آلية إنفاذ. لا بند «مراجعة أثر رجعي» في قوالب المراحل |
| 7.2 | **L-18** | القسمة على المتوازي لا الضرب بها | حراسة كوديّة داخلية فقط. لا قاعدة عامة لكل حساب زمني قادم |
| 7.3 | **L-19** | BullMQ 5 يفصل `prioritized` عن `waiting` | لا استشهاد في PHASES ولا قاعدة في `docs/08 §الطوابير` |
| 7.4 | **L-20** | Next.js + workspace ESM يحتاجان webpack alias | معرفة موضعية في `next.config.mjs` فقط. لا قاعدة لكل Next.js app قادم |
| 7.5 | **L-22** | رسائل الخادم مفاتيح لا نصوصاً | لا قاعدة i18n في CLAUDE ولا وثيقة API |
| 7.6 | **L-23** | المركّبات الرقمية تحت RTL تحتاج `dir="ltr"` | معرفة داخل `<Ltr>` component فقط. لا فحص lint |
| 7.7 | **L-25** | البوابات الكمّية لا تكشف الخلل النصّي | يحيل داخلياً إلى L-16/L-17. لا بند إجرائي «راجع كل النصوص في كل الأوضاع» |
| 7.8 | **L-33** | التوجيه المبني على معلومة خاطئة يُوقَف لا يُنفَّذ | **الأكثر إلحاحاً:** بوابة توقّف تماماً مثل L-32 المطبَّق في CLAUDE.md — لكنها غير مسجَّلة في §بوابات التوقّف |
| 7.9 | **L-36** | الرقم يقيس أثر الميزة لا متوسط المجموعة | القاعدة «قسّم إلى تدخّلت/لم تتدخّل» غير مضافة إلى §سجل الأصول التسويقية في CLAUDE.md رغم مساسها المباشر بكل رقم يُنشر |

**ملاحظة على «المُطبَّق»:** L-14, L-16, L-17, L-21, L-27, L-30, L-31
مسجَّلة عبر إحالات من `docs/12, 13, 14, 15` لكنها **لا تمرّ في CLAUDE.md**
الذي يُقرأ في كل جلسة. جلسة لا تفتح مسار الميزة المعنيّة قد لا تراها.

---

## 8. بنود مؤجَّلة بلا شرط إعادة فتح كامل

من 39 بنداً مؤجَّلاً/معلَّقاً، **21 بنداً** بها فجوة:

**«لا سبب ولا شرط» (6 بنود):**
| # | البند | مكان |
|---|---|---|
| 8.1 | لقطات مرجعية للقوالب الأربعة | PHASES:209 |
| 8.2 | لقطة مقارنة مع Canva | PHASES:564 |
| 8.3 | كشف الوجوه للقصّ الذكي | PHASES:711 |
| 8.4 | التعليق الصوتي بمفتاح العميل | PHASES:719 (السبب تقني لا سبب تأجيل، ولا مُشغِّل) |
| 8.5 | كل بنود المرحلة 5 (6 بنود مجمَّعة) | PHASES:896-907 |
| 8.6 | كل بنود مسار العميل الأول (7 بنود) | PHASES:913-920 (قيد الانتظار بلا مُشغِّل مسجَّل) |

**«شرط ضبابي» (9 بنود):**
| # | البند | الشرط الضبابي |
|---|---|---|
| 8.7 | تلوين watermark (tint) | «حتى أول عميل بشعار حقيقي» — بلا معيار «حقيقي» |
| 8.8 | تنزيل 7 خطوط + OFL | «المرحلة 4» — بلا حدث داخلها |
| 8.9 | MP4 على iPhone/أندرويد + إنستغرام | «يحتاج جهاز عميل حقيقي» — متى؟ |
| 8.10 | دَين 128ms (كسر دلالي) | لا مُشغِّل — هل يعود عند شكوى أداء؟ |
| 8.11 | إعادة تشغيل العامل تلقائياً | مقفل على main — لا شرط لفك القفل |
| 8.12 | قتل مهمة نشطة | نفس أعلاه |
| 8.13 | ويب هوك التنبيهات | «cron أو BullMQ repeat» — احتمالان بلا اختيار |
| 8.14 | مراجعة تصميمية للقوالب الستة | «التوقيت يحدده المالك» = لا حدث مُشغِّل |
| 8.15 | SDK معاينة في المتصفح | «لمن يبني فوقنا» — كم مشتركاً؟ |
| 8.16 | قوالب متعددة الصفحات | «لم يظهر طلب» — كم طلباً يفتحه؟ |

**«سبب مفقود» (1 بند):**
| # | البند |
|---|---|
| 8.17 | `WebCodecs` للفيديو القصير — «يُرفع مع الواجهة» بلا سبب معماري |

**البند القدوة:** Lottie (PHASES:695-709) — سبب معماري محدَّد (Skottie
غير مكشوف في skia-canvas 3.0.8) + شرط إعادة فتح صريح (إصدار جديد يكشف
Skottie). **قالب يُقارَن به بقية البنود.**

---

## توصية ترتيب المعالجة (من الأثر إلى الجهد)

1. **6-ب (docs/04 vs templates)** — أعمق فجوة اكتمال. مطوّر قوالب جديد
   سيقرأ وثيقة قديمة كلياً. جدول الطبقات + `clipstream` الموعود +
   الأمثلة الخاطئة تحتاج إعادة كتابة جوهرية.
2. **6-أ (docs/03 vs brand-kit.ts)** — الافتراضات المقلوبة (6.8–6.10)
   تعني أن مطوّراً يبني هوية جديدة على docs/03 سيتفاجأ بأن DEFAULT_BRAND
   يحمل قيماً معاكسة.
3. **5.5 (طبقة caption على main)** — بعد الدمج، تحقّق: هل شحّن الكود
   نفسه غيّر PHASES.md لتعكس البناء؟ إن لم يفعل، الدَين ينمو.
4. **7.8 (L-33 بوابة توقّف)** — قاعدة سلوكية حرجة يجب أن تنتقل إلى
   §بوابات التوقّف في CLAUDE.md.
5. **5.10 و 5.11 (LRI/PDI و AudioPlan)** — قراران معماريان مسجَّلان
   كتعليقات كوديّة تستحق ترقية إلى ADR.
6. **5.13 (easing set change)** — تناقض بين الكود وسجل PHASES يحتاج
   سطر قرار «حذفنا Quad/Cubic وأضفنا spring/step لأنّ...».
7. **باقي المحور 5** (شجرة `docs/02`) — تحديث ميكانيكي.
8. **المحور 8 (شروط إعادة فتح)** — قابل للمعالجة بدفعة واحدة: كل بند
   يستحق سطراً «يعود عند X».
9. **المحور 7 (الدروس)** — كل درس بلا تطبيق يستحق قراراً: هل هو قاعدة
   أم ملاحظة؟ الملاحظات تُنقَل خارج LESSONS.md (لا تُحذف).

---

# فحص أعمق — تنفيذاً لتوجيه المالك (2026-09-03، دفعة ثانية)

## 9. تصنيف الدروس بلا أثر — بأمانة، بلا مجاملة

من 38 درساً في `docs/LESSONS.md`، **19 درساً** لا يظهر أثرها في CLAUDE.md
ولا في PHASES.md ولا كبوابة/سكربت (9 «بلا تطبيق» + 7 «إحالة من docs
الفرعية فقط» + 3 مراجعة نقدية إضافية أضفتها بعد قراءة صادقة).

**قاعدة التصنيف — بلا مجاملة:**
- **A (قاعدة في CLAUDE.md):** درس يحكم سلوك كل جلسة. غيابه عن CLAUDE.md
  يعني أن جلسة قد تكرّر الخطأ.
- **B (بند في PHASES.md أو وثيقة عملية):** درس يفرض عملاً محدَّداً
  في نطاق ضيّق (طوابير، ops، app بعينه). لا يستحق مساحة CLAUDE.md
  ولكنه يستحق سطر إنفاذ في مكانه.
- **C (ملاحظة تاريخية):** كُتب لأن الحادثة بدت مهمة لحظتها لا لأن
  النمط يتكرّر. صادق أن يُنقل خارج LESSONS.md (إلى ملحق تاريخي)
  لا يُحذف.

### الجدول — تصنيف صريح

| # | الدرس | العنوان الموجز | التصنيف | السبب الصادق |
|---|---|---|---|---|
| L-08 | التصنيف النحوي ≠ الحاجة التيبوغرافية | **C** | بصيرة موضعية عن الكسر الدلالي — النمط لا يتكرّر (قرار طباعي حُسم مرة) |
| L-11 | تحقّق من أداة القياس قبل النظام | **C** | حرفة عامة للمبرمج المحترف، لا خصوصية للمشروع. تكرارها في LESSONS يضعف بقية الدروس |
| L-14 | التصميم لكل وحدة صغيرة يشتري مرونة | **C** | مبدأ برمجي مجرَّد — يُطبَّق عبر الذوق لا القاعدة. صعب أن تُترجَم إلى إنفاذ |
| L-15 | تغيير قرار متأخر يوجب مراجعة ما سبقه | **B** | عمل محدَّد يستحق «قائمة تحقّق مراجعة أثر رجعي» في قالب المراحل — لا قاعدة عامة في CLAUDE |
| L-16 | البوابة الآلية لا تكشف التصادم البصري | **A** | يحكم سلوك: كل بوابة بصرية تحتاج مراجعة عين — قاعدة كل جلسة |
| L-17 | لا تُعلَن بوابة مخرج بصري ناجحة قبل النظر إلى المخرج | **A** | نفس L-16 بصياغة سلوكية أوضح. جلسة قد تكرّر بلا هذه القاعدة |
| L-18 | القسمة على المتوازي لا الضرب بها | **B** | خطأ رياضي ضيّق (حسابات الطوابير) — يستحق سطر في `docs/08 §الطوابير`، لا قاعدة عامة |
| L-19 | BullMQ 5 يفصل prioritized عن waiting | **B** | خصوصية API — يستحق سطر في `docs/08` أو تعليق في `apps/renderer/src/queues.ts`. غير عام |
| L-20 | Next.js + workspace ESM يحتاجان webpack alias | **B** | خصوصية إعداد — يستحق قسم في `docs/11` أو ملف «app setup» عند بدء المرحلة 4 |
| L-21 | «الصيانة» تعني إيقاف الطوابير، لا مجرّد علم | **B** | خصوصية ops — يستحق `docs/08 §الصيانة` |
| L-22 | رسائل الخادم مفاتيح لا نصوصاً | **A** | يحكم كل تصميم API — إن لم يكن في CLAUDE.md، Studio في المرحلة 4 سيكرّر الخطأ |
| L-23 | المركّبات الرقمية تحت RTL تحتاج dir="ltr" | **B** | خصوصية `apps/dashboard` (ومستقبلاً Studio) — يستحق مكاناً في وثيقة مكوّنات الواجهة، لا CLAUDE |
| L-24 | قاعدة اللغة المختلطة تُختبَر بالقراءة بصوت عالٍ | **C** | طريقة اختبار موثَّقة موضعياً — القاعدة صادرة عن حادثة واحدة، مطبَّقة الآن في اختبارات BiDi |
| L-25 | البوابات الكمّية لا تكشف الخلل النصّي | **C** | هي إعادة صياغة لـL-16 + L-17 (يقوله الدرس نفسه). ثلاثة دروس على نفس المبدأ = دَين توثيق، لا ثلاث قواعد |
| L-27 | التصنيف الأول للميزة قد يكون خاطئاً — راجعه | **A** | يحكم كل قائمة «مؤجَّل / مرفوض» — قاعدة مراجعة دورية تستحق CLAUDE. غيابها هو سبب المحور 8 نفسه |
| L-30 | أداة الفريق ليست أداة فردية بواجهة مشتركة | **C** | فلسفة منتج، مكانها الطبيعي `docs/01-product.md` لا LESSONS. أنا وضعتها في LESSONS لأنها اكتُشفت من حادثة — لكنّها ليست «درساً هندسياً» |
| L-31 | اسأل عن العمل الجاري لا عن الميزات المطلوبة | **C** | نفس L-30 — طريقة اكتشاف عميل، مكانها docs/01 أو docs/15، لا LESSONS |
| L-33 | التوجيه المبني على معلومة خاطئة يُوقَف لا يُنفَّذ | **A** | بوابة توقّف مطابقة لـL-32 المُطبَّقة. غيابها عن §بوابات التوقّف في CLAUDE = فجوة صريحة |
| L-36 | الرقم يقيس أثر الميزة لا متوسط المجموعة | **A** | يحكم كل رقم يُنشَر في M1/M2/مادّة تسويقية — قاعدة تسويقية عالية الأثر |

### الملخّص العددي

- **إجمالي بلا أثر:** 19 (من 38)
- **A — يستحق قاعدة في CLAUDE.md:** **6** — L-16, L-17, L-22, L-27, L-33, L-36
- **B — يستحق بنداً/سطراً في وثيقة عملية:** **6** — L-15, L-18, L-19, L-20, L-21, L-23
- **C — ملاحظة تاريخية فقط (تُنقل إلى ملحق):** **7** — L-08, L-11, L-14, L-24, L-25, L-30, L-31

**الصدق:** سبعة دروس من التسعة عشر (37%) هي **ملاحظات لا قواعد**. أنا كتبتها
لأن الحادثة أثّرت فيّ لحظتها. النمط لا يتكرّر — أو يتكرّر بشكل يحتاج
مبدأ برمجياً عاماً لا خصوصية للمشروع.

**L-30 و L-31 خصوصاً** فلسفتا منتج، اكتُشفتا في تحوّل موقعي «أداة فردية →
أداة فريق». مكانهما الطبيعي `docs/01-product.md` مع قرار التموضع الجديد.
كتبتُهما في LESSONS لأن العملية شعرت كدرس، لكنّهما لا يمنعان خطأ برمجياً
تكرارياً — يشكّلان مواصفة منتج.

**L-25 خصوصاً** تعليق نصّي لطيف على L-16 و L-17. الأصح لغةً «امتداد
L-16/L-17» في نفس الدرسين، لا درس مستقلّ.

**اقتراح للمالك:**
- ترقية الستة A إلى CLAUDE.md دفعة واحدة (§قواعد أو §بوابات التوقّف
  حسب الحالة).
- إدراج الستة B في وثائقها الطبيعية (docs/08 · docs/11 · مكوّنات
  الواجهة).
- نقل السبعة C إلى ملحق تاريخي (`docs/LESSONS-archive.md` مثلاً)
  مع سطر «كُتبت لحظة الحادثة، النمط لا يتكرّر». لا حذف — تاريخ
  التوثيق مفيد للمراجعات المستقبلية.

---

## 10. البنود المؤجَّلة — اقتراح شرط إعادة فتح لكل بند

من 21 بنداً مؤجَّلاً بفجوة شرط (المحور 8 السابق). لكل واحد، **اقترح
شرطاً من السياق** أو أكتب «يحتاج قرار مالك» إن لم يكن السياق كافياً.

**قاعدة الاقتراح:** لا اختراع — الشرط يخرج من نصّ البند أو من مرحلة/بوابة
تالية أو من حدث خارجي محدد.

### 10-أ. بلا سبب ولا شرط (6)

| # | البند | مكانه | شرط مقترح |
|---|---|---|---|
| 10.1 | لقطات مرجعية للقوالب الأربعة | PHASES:209 | **«يُنجَز مع تحويل القوالب إلى JSON في المرحلة 2»** — المرحلة انتهت والبند مُغلق فعلاً (24 لقطة في `snapshots/` + `snapshots-semantic/`). البند نفسه ميت — يستحق ☑ لا ☐ |
| 10.2 | لقطة مقارنة مع Canva | PHASES:564 | **«قبل أول عرض تسويقي علني (M2)»** — البند تسويقي، مكانه الطبيعي M2 قائمة قصيرة يفتحها الاحتفال بالإطلاق |
| 10.3 | كشف الوجوه للقصّ الذكي | PHASES:711 | **«يحتاج قرار مالك»** — لا يوجد شرط تقني يفرض ترتيباً؛ داخل المرحلة 3.8 لكن أيّ حدث يُشغّله؟ |
| 10.4 | التعليق الصوتي بمفتاح العميل | PHASES:719 | **«عند اتفاق مع مزوّد BYO-key (ElevenLabs أو Google TTS أو Azure) — يحدَّد أوّل مزوّد يُتَكامل معه»** |
| 10.5 | كل بنود المرحلة 5 (6) | PHASES:896-907 | **«يُعاد فتحها بعد بوابة المرحلة 4 (عميل يعمل ذاتياً)»** — منطق العمود الفقري: 4 → 5 |
| 10.6 | مسار العميل الأول (7 بنود) | PHASES:913-920 | **«يبدأ عند اكتمال بوابة المرحلة 3.8 + بند SAMPLE_ONLY تقنياً في المرحلة 4»** — العميل يحتاج امتدادات المحرك جاهزة + علامة النسخة التجريبية |

### 10-ب. شرط ضبابي (10)

| # | البند | الشرط الحالي | شرط ملموس مقترح |
|---|---|---|---|
| 10.7 | تلوين watermark (tint) | «حتى أول عميل بشعار حقيقي» | **«عند رفع أول عميل شعاراً ملوَّناً (لا رمادي أبيض) — يُقاس من `brands/*.json` غير `client-demo`»** |
| 10.8 | تنزيل 7 خطوط + OFL | «المرحلة 4» | **«عند بدء بناء محرّر Brand Kit في Studio — البند الأوّل فيه»** |
| 10.9 | MP4 على iPhone/أندرويد + إنستغرام | «يحتاج جهاز عميل حقيقي» | **«قبل أول تسليم رسمي في مسار العميل الأول (البند 10.6 §خمس بطاقات وفيديو)»** — التسليم يتطلّب الاختبار |
| 10.10 | دَين 128ms (كسر دلالي) | لا شرط | **«يُعاد الفحص عند أول شكوى أداء من عميل، أو عند مضاعفة حجم القوائم (`entities` أو `places`) في `data/external/`»** |
| 10.11 | إعادة تشغيل العامل تلقائياً | «مقفل على main» | **«يحتاج قرار مالك»** — القفل نفسه بلا شرط فك؛ التقني: تكامل Redis pub/sub في `worker.ts` |
| 10.12 | قتل مهمة نشطة | نفس أعلاه | **«يحتاج قرار مالك»** — نفس المسار (فك قفل worker.ts) |
| 10.13 | ويب هوك التنبيهات | «cron أو BullMQ repeat» | **«يحتاج قرار مالك — اختيار واحد من الاثنين قبل البدء»** |
| 10.14 | مراجعة تصميمية للقوالب الستة | «التوقيت يحدده المالك» | **«يُعاد فتحه عند وصول العميل الثاني (تغطية استعمال حقيقي متعدد)»** — يمنع «مراجعة أبدية بلا حدث» |
| 10.15 | SDK معاينة في المتصفح | «لمن يبني فوقنا» | **«عند أول طلب مؤسسي من عميل يبني منتجاً — يُعرَّف كطلب رسمي عبر البريد»** |
| 10.16 | قوالب متعددة الصفحات | «لم يظهر طلب» | **«عند طلب من ثلاثة عملاء مختلفين — يُسجَّل في تذكرة تراكم الطلبات»** |

### 10-ج. سبب مفقود (1)

| # | البند | الحالي | مقترح للسبب |
|---|---|---|---|
| 10.17 | WebCodecs للفيديو القصير | «يُرفع مع الواجهة» — بلا سبب معماري | **السبب:** «معاينة سريعة في المتصفح دون جولة كاملة عبر الخادم — يُنفَّذ عند بدء واجهة Studio التي تحتاج إلى معاينة إطارات بلا رحلة كاملة» |

### 10-د. البنود ذات الشرط الملموس بالفعل (لا فجوة — للتذكير)

| البند | الشرط |
|---|---|
| Lottie / Skottie | إصدار جديد من `skia-canvas` يكشف Skottie ✓ (القالب) |
| WojoodGaza (بوابتان أ/ب) | وصول المجموعة عبر sina.birzeit.edu ✓ |
| فك الترميز المسبق | مقطع مصدر حقيقي من العميل الأول ✓ |
| سجل التصديرات + الاستهلاك | توفّر PostgreSQL ✓ |
| brand.locale | مع هوية العميل الأول ✓ |

**الخلاصة:** من 17 بنداً بفجوة، **11 يقبلون شرطاً من السياق**، و**6 تحتاج
قرار مالك حقيقياً** (10.3 · 10.11 · 10.12 · 10.13 + شرط فك القفل، ومنطق
لا يستقيم إلا بحكم). القرارات ليست معقدة، لكنّها ليست مشتقّة من نصّ.

---

## 11. مسودة ADR-010 — النموذج الزمني يُبنى من جديد لا يُوسَّع

> **ملكية:** `docs/02-architecture.md` مقفول على `main` — لا أكتبه هنا.
> النصّ أدناه اقتراح يُدرَج بعد الدمج.

```markdown
## ADR-010: النموذج الزمني يُبنى من جديد لا يُوسَّع (2026-09-02)

**السياق:** المرحلة 3.7 تطلبت محرّك خط زمني كامل (بمسارات ومفاتيح
مفتاحية ودوال easing وانتقالات وطبقات صوت). الأداة القديمة تحمل
`cvSegTimeline` — تسلسل مقاطع مسطَّح مع animation array — بُنية
كافية لبطاقة عاجل من صور ثابتة، عاجزة عن مقاطع منزلقة أو صوت
مُتزامن أو مسارات نصّية موازية.

**القرار:** بناء `packages/engine/src/timeline-v2/` **كاملاً بمعزل**
عن `cvSegTimeline`، مع بوابة تكافؤ **253/253** اختبار قبل حذف القديم.
النموذج الجديد يحمل:
- `Track[]` (media · text · audio) — مسارات موازية
- `Keyframe[]` لكل خاصية قابلة للتحريك
- `AudioPlan` منفصلة عن `drawTimelineAt` (خالصة كوسيط)
- ثمانية دوال easing (linear · easeIn/Out/InOut · easeOutBack · spring · step)
- خمسة انتقالات (crossfade · slide · wipe · zoom · blurIn)

**البدائل المرفوضة:**

1. **توسيع `cvSegTimeline` بالمسارات الجديدة:** يورث ديون
   البنية القديمة (كل مقطع يحمل حقول animation ثابتة، لا نموذج
   عام قابل للامتداد). كل ميزة جديدة تحتاج حقلاً جديداً في كل مقطع —
   انفجار سطح البيانات.

2. **بناء `v2` داخل نفس المكان مع flag `useV2`:** يعرّض `drawTimelineAt`
   لفرعين شرطيين، يكسر النقاء الزمني (خط تنفيذ يختلف بحسب الإعدادات)،
   ويعقّد اختبار التكافؤ (لا يمكن تشغيل v1 و v2 على نفس المدخل بلا تدخّل).

3. **حذف `cvSegTimeline` وبناء v2 محلّه مباشرة:** يترك المشروع بلا
   نموذج زمني عامل حتى يكتمل v2 (أسابيع). البدائل موجودة (v1 يعمل)،
   وحذفها بلا بديل يخالف قاعدة الخنق التدريجي.

**المقايضة:** حجم كود مضاعف مؤقتاً (~1500 سطر إضافي)، تكلفة مراجعة
أعلى، خطر «drift» بين v1 و v2 (تُخفَّف ببوابة التكافؤ الاختبارية).
المكسب: نموذج نظيف قابل للامتداد بدلاً من طبقات patches فوق بنية
قديمة.

**شرط الجودة (بوابة الاستبدال):** حذف `cvSegTimeline` مسموح **فقط**
بعد اجتياز 253/253 اختبار في `timeline/*.test.ts` (نفس المدخلات تعطي
نفس المخرجات bytecoded — بايت-بايت في MP4 المُصدَّر عبر `verify-breaking-video.mjs`).
اختبار التكافؤ (`verify-timeline-equivalence.mjs`) بقي حتى قرار الحذف
(2026-09-02) ثم حُذف — نتيجته موثَّقة في PHASES §المرحلة 3.7.

**نتيجة:** v2 مدموج في المرحلة 3.7 (2026-09-02)، v1 محذوف كلياً
(~1000 سطر)، `templateToTimeline` هو الجسر، `snapshots-video/` مرجع
دائم يحرس أن أيّ regression في v2 يُكشَف.
```

**النمط المستحقّ الترقية إلى ADR أيضاً** (اكتُشف في المحور 5.10-5.11):

- **ADR-011 مقترح:** استعمال `LRI/PDI` (U+2066/U+2069) بدل `LRM` (U+200E)
  للعزل. التبرير مكتوب كتعليق داخل `attribution.ts` — يستحق ترقية.
- **ADR-012 مقترح:** `AudioPlan` قيمة JSON قابلة للتسلسل، منفصلة عن
  `drawTimelineAt`. التبرير مكتوب كتعليق داخل `audio-graph.ts` — نفس النمط.

---

## 12. البوابات المُعلَنة بلا اختبار دائم يحرسها

**الفكرة:** بوابة تحقَّقت مرة يدوياً ولم يبقَ لها اختبار دائم = **بوابة
هشّة**. أيّ regression لن يُكشَف حتى مراجعة يدوية تالية.

**المُحفِّز اليوم:** اختبار «ثبات الكشيدة» في طبقة caption أثبت الثبات
داخل استدعاء واحد لكنّه لم يكشف الاختلاف عبر أنماط highlight. **بوابة
كانت أضيق من ادّعائها.**

### البوابات المُعلَنة ☑ في PHASES.md — تقييم كل واحدة

**البوابات ذات اختبار دائم واضح (12):**

| البوابة | السكربت الحارس |
|---|---|
| نقاء المحرك | `scripts/check-engine-purity.mjs` (يعمل مع كل `pnpm test`) |
| 24 لقطة ذهبية | `scripts/verify-snapshot.mjs` |
| MP4 عاجل بايت-بايت | `scripts/verify-breaking-video.mjs` (mp4 + md5) |
| الإطار عند t=1.4 | `scripts/verify-frame-at.mjs` |
| مسارات الميديا (timeline v2) | `scripts/verify-media-track-gate.mjs` |
| مسارات النصّ (timeline v2) | `scripts/verify-text-tracks-gate.mjs` |
| الانتقالات | `scripts/verify-transitions-gate.mjs` |
| الصوت (AudioPlan) | `scripts/verify-audio-gate.mjs` |
| هوية الحدود | `scripts/verify-boundary-identity.mjs` |
| ملء اللف | `scripts/verify-wrap-fidelity.mjs` |
| الذروة (9 مهام) | `scripts/test-peak-load.mjs` |
| عزل الفشل | `scripts/test-isolation.mjs` |
| دقة ETA لوحة التحكم | `scripts/dashboard-eta-check.mjs` |

### البوابات ذات فجوة اختبار (المشتبَه فيها)

| # | البوابة المُعلَنة | حالة الاختبار | ملاحظة |
|---|---|---|---|
| 12.1 | **Phase 0:** «بحث نصي في main لا يُظهر أي أصل للأناضول» (`AA_LOGO`, `CV_LOGO`, `HelveticaNeueLTArabic`, `HNArabic`, `AA-60-SANIYE`) | **يدوي مرة واحدة** | لا سكربت دائم يشغّل `grep` لكل الرموز المحظورة. أيّ commit جديد قد يُعيد إدراج أحدها بلا كشف. **يستحق `scripts/verify-clean-of-aa.mjs`** |
| 12.2 | **Phase 1:** «`git diff HEAD packages/ = صفر` بعد إضافة `client-demo.json`» | **assertion لحظية** | تحققت مرة في 2026-08-31، لا سكربت دائم يعيدها عند إضافة brand جديد. **يستحق فحص في CI عند add-brand** |
| 12.3 | **Phase 1:** «مؤتمر Brussels للسلام يظهر بترتيب صحيح» (BiDi) | **يوجد `bidi.test.ts`** | مُغطّى — لا فجوة |
| 12.4 | **Phase 2:** «قالب خامس بلا كود» (plain.json) | **assertion لحظية** | تحقّقت مرة (صفر تعديل في `packages/engine/`)، لا سكربت دائم يمنع regression عند إضافة قالب سادس |
| 12.5 | **Phase 3:** «MP4 يُشغَّل على macOS» (ffprobe) | **يدوي عبر ffprobe** | لا اختبار آلي؛ الاعتماد على ffmpeg نفسه لإخراج MP4 صالح. `verify-breaking-video.mjs` يفحص bytes لا التوافق مع مشغّلات |
| 12.6 | **Phase 3:** «MP4 على iPhone/أندرويد + رفع لإنستغرام» | **⏸ مؤجَّل** — لا اختبار حتى الآن | مُقرّ صراحةً في PHASES:332 |
| 12.7 | **Phase 3.5 بوابة ⏱:** `p95 ≤ 800ms` لـbuildRenderPlan | **قياس لحظي** | مُقاس مرة (402ms)، لا سكربت دائم يُعيده. بحاجة `scripts/verify-buildRenderPlan-perf.mjs` |
| 12.8 | **Phase 3.5 بوابة ج/د:** مقاييس على 265 عنوان RSS | **`scripts/eval-semantic-rss.mjs` موجود** | مُغطّى — لا فجوة |
| 12.9 | **Phase 3.7 (drawAt خالصة):** 6 أوقات بترتيبين — نفس الـops | **يوجد `timeline.test.ts`** | مُغطّى — لا فجوة |
| 12.10 | **Phase 3.7 (breaking-md5):** MP4 breaking متسق | **`verify-breaking-video.mjs`** | مُغطّى |
| 12.11 | **Phase 3.8 (طبقة الإسناد):** «24/24 لقطة مستقرّة (attribution ينتظر content ولا يظهر بدونه)» | **مُغطّى بـverify:snapshot** | البوابة العامة صحيحة، لكن **الاختبار المحدَّد** «يظهر مع content، يختفي بلا content» ليس له سكربت مخصّص — يعتمد على أن snapshots الحالية تغطّي الحالتين |
| 12.12 | **Phase 3.8 (caption — على main):** «ثبات الكشيدة داخل caption» | **ضيّق النطاق (المُحفِّز اليوم)** | الاختبار الحالي يفحص ثبات ضمن استدعاء واحد، لا عبر أنماط highlight الخمسة. **مُقرّ من المالك كفجوة اليوم** |

### الأنماط الملاحَظة

1. **البوابات الظرفية (one-shot):** #12.1 · #12.2 · #12.4 · #12.5 · #12.7
   تحقّقت مرة يدوياً بلا سكربت دائم. أيّ regression خارج الجلسة التي
   تحقّقت فيها يمرّ صامتاً.

2. **البوابات الضيّقة النطاق:** #12.11 · #12.12 — الاختبار يغطّي حالة
   واحدة من مجموعة. البوابة تُعلَن عامّة والاختبار خاصّ.

3. **البوابات القوية:** الطوابير · النقاء · اللقطات · timeline v2 — كلها
   مغطّاة بسكربتات `verify-*.mjs` أو اختبارات vitest. **12 من 24 = 50%**.

**اقتراح:** كل بوابة تُعلَن ☑ في PHASES.md **يجب أن تُرفَق بمسار سكربت
حارس**. الشكل المقترح:

```
البوابة (متحقّقة 2026-XX-XX):
1. ☑ <النصّ> — **حارس دائم:** `scripts/verify-<name>.mjs`
2. ☑ <النصّ> — **حارس دائم:** `packages/<pkg>/src/<x>.test.ts`
3. ☑ <النصّ> — **حارس دائم:** ⚠ لا يوجد، تحقّق يدوي فقط (أضف: سبب)
```

الشكل الأخير يجعل الفجوة **مرئية** بدل أن تختفي تحت ☑.

---

## سجل الدفعة الثانية (2026-09-03)

**عولج في هذه الدفعة:**
- ✅ إضافة `brand.assets.version` كبند تنفيذي في `PHASES.md §المرحلة 4`
- ✅ تصنيف 19 درساً بلا أثر (A: 6 · B: 6 · C: 7)
- ✅ اقتراح شرط إعادة فتح لكل بند مؤجَّل (11 من السياق · 6 تحتاج قرار مالك)
- ✅ مسودة ADR-010 (النموذج الزمني) + اقتراح ADR-011 (LRI/PDI) + ADR-012 (AudioPlan)
- ✅ فحص 24 بوابة معلَنة — **12 مُغطّاة بسكربت دائم · 7 ذات فجوة اختبار**

**تُرك للمالك:**
- ترقية الستة A إلى CLAUDE.md (يمسّ ملكية main)
- إضافة الستة B إلى وثائقها الطبيعية (docs/08 · docs/11 · وثيقة مكوّنات الواجهة عند بناء Studio)
- نقل السبعة C إلى `docs/LESSONS-archive.md` أو ملحق مماثل
- اعتماد النصّ المقترح لـADR-010 (يمسّ docs/02)
- كتابة السكربتات الحارسة السبعة المفقودة (خارج نطاق docs/)

---

# فحص أعمق — دفعة ثالثة (2026-09-03)

## 13. خطة تحويل البوابات غير المحروسة

**السياق:** تسع بوابات مُعلَنة ☑ في PHASES.md بلا حارس دائم — أيّ
انحدار يمرّ صامتاً. اكتشاف اليوم (اختبار كشيدة caption ضيّق النطاق)
كشف بنية المشكلة: البوابة تُعلَن كتحقّق شامل، والاختبار يحرس زاوية
واحدة.

**تصنيف كل بوابة بحسب L-46:**
- **وجود (existence):** المدخلات تُغطّي نطاق الاستعمال المُدَّعى.
- **ثبات (stability):** نفس المدخل يُنتج نفس المخرج على استدعاءين.
- **كلاهما:** الوجود عبر النطاق + الثبات داخل كل مدخل.

### الجدول

| # | البوابة | ما تدّعيه | أتمتة؟ | السكربت المطلوب | جهد | نوع الاختبار (L-46) |
|---|---|---|---|---|---|---|
| 13.1 | **12.1** Phase 0 — «لا أصل أناضول في main» | 6 رموز محظورة (`AA_LOGO`, `CV_LOGO`, `HelveticaNeueLTArabic`, `HNArabic`, `AA-60-SANIYE`, `aa_customLogo`) لا تظهر في أيّ ملف مصدر | **نعم — كامل** | `scripts/verify-clean-of-aa.mjs`: `git grep` عن الرموز الستة (باستثناء docs/INVENTORY.md المُوسَم تاريخياً + docs/LESSONS.md)، exit 1 عند أيّ مطابقة | صغير (نصف ساعة) | **وجود** — عبر النطاق: كل ملف مصدر × كل رمز محظور |
| 13.2 | **12.2** Phase 1 — «`git diff HEAD packages/ = 0` عند إضافة brand» | إضافة `brands/<X>.json` تُنتج مخرجاً جديداً بلا لمس `packages/` | **جزئي** — يحتاج قالب اختبار | `scripts/verify-brand-isolation.mjs`: يستقبل `--new-brand=<path>`، يُشغّل preview قبل وبعد، يفحص `git diff --exit-code packages/`. يعمل في CI عند مسار `brands/*.json` جديد | متوسط (2–3 ساعات) | **وجود** — على كل brand جديد يُدخَل |
| 13.3 | **12.4** Phase 2 — «قالب خامس بلا كود» | إضافة `packages/templates/src/templates/<X>.json` تُنتج قالباً يعمل بلا لمس `packages/engine` | **جزئي** — امتداد لـ13.2 | `scripts/verify-template-isolation.mjs`: على أي ملف قالب جديد، يشغّل preview + verify:snapshot + `git diff --exit-code packages/engine` | متوسط (2–3 ساعات) | **وجود** — كل قالب × كل هوية |
| 13.4 | **12.5** Phase 3 — «MP4 يُشغَّل على macOS» | المخرَج MP4 صالح تقنياً: H.264 + yuv420p + AAC + bt709 + faststart | **نعم — كامل** | `scripts/verify-mp4-conformance.mjs`: يُشغّل `ffprobe` على `snapshots-video/breaking.mp4`، يتحقّق من codec/pix_fmt/color_primaries/moov position | صغير (ساعة) | **وجود** — على كل mp4 مرجعي |
| 13.5 | **12.6** Phase 3 — «MP4 على iPhone/أندرويد + رفع إلى إنستغرام» | يعمل على أجهزة الاستهلاك الفعلية | **لا** — يحتاج جهاز حقيقي | البديل: **قائمة يدوية دورية** — قبل كل تسليم لعميل جديد. تُوثَّق في `docs/09 §القبول قبل التسليم` كخطوة إلزامية | صغير (تعديل وثيقة) | **وجود يدوي** — لا يمكن أتمتته بلا مزرعة أجهزة |
| 13.6 | **12.7** Phase 3.5 — «`buildRenderPlan` p95 ≤ 800ms» | الأداء لا يتراجع مع القوائم الحالية (places 72KB + entities 113KB + titles 3KB) | **نعم — كامل** | `scripts/verify-render-plan-perf.mjs`: يُشغّل `buildRenderPlan` 100 مرة على نص عاجل قياسي، يحسب p95، exit 1 إن > 800ms | متوسط (1–2 ساعة) | **ثبات** — نفس المدخل، ثبات الأداء عبر تشغيلات |
| 13.7 | **12.11** Phase 3.8 — «attribution يظهر مع content ويختفي بدونه» | الطبقة موجودة عند وجود بيانات، غائبة عند غيابها | **جزئي** — يعتمد على snapshots الحالية | يستحق سكربت مخصّص: `scripts/verify-attribution-conditional.mjs`: يرندر بطاقة بلا `content.attribution` ويتحقّق أن `renderFrame` لم يستدعِ `attribution.ts`، ثم بـcontent وتحقّق أنه استدعاها | متوسط (2 ساعة) | **كلاهما** — وجود (مع + بلا) وثبات على كليهما |
| 13.8 | **12.12** Phase 3.8 — «ثبات الكشيدة في caption» (المُحفِّز اليوم) | مواضع التطويل ثابتة عبر أنماط highlight الخمسة | **نعم — كامل** | `scripts/verify-caption-kashida-stability.mjs`: نصّ ثابت × 5 أنماط highlight × 5 استدعاءات، يتحقّق أن kashidaSites متطابقة عبر (١) الأنماط الخمسة (وجود) و(٢) الاستدعاءات الخمسة لكل نمط (ثبات) | متوسط (2–3 ساعات) | **كلاهما** — الفراغ اليوم كان الوجود عبر الأنماط، والاختبار حرس الثبات فقط |
| 13.9 | **إضافي:** Phase 1 — «مؤتمر Brussels للسلام يظهر بترتيب صحيح» | BiDi يعالج المقاطع اللاتينية داخل عربي | **مُغطّى بـ`bidi.test.ts`** لكن على عبارة واحدة | امتداد الاختبار الحالي: مصفوفة عبارات (10 عبارات مختلطة تُغطّي أنماط الترقيم والأقواس والأرقام) | متوسط (1–2 ساعة) — امتداد لا سكربت جديد | **وجود** — عبر نطاق التنسيقات المختلطة |

### ملخّص الجهد

- **صغير (< ساعة)** — 3 بنود (13.1, 13.4, 13.5)
- **متوسط (1–3 ساعات)** — 6 بنود (البقية)
- **إجمالي مقدَّر:** يوم عمل واحد لكاتب سكربتات مركَّز.

### الأنماط المستخلَصة من L-46

**البوابات التي كانت «ثبات» يجب أن تصير «كلاهما» (2):**
- 13.7 (attribution) — كنّا نعتمد أن snapshots تغطّي الوضعين، حتى لو لم تكن مقصودة له تحديداً.
- 13.8 (kashida stability) — كنّا نعتمد أن الثبات يعني الاتساق، بينما الاتساق عبر modes خاصية مختلفة.

**البوابات التي تحتاج «وجود عبر نطاق» فقط (5):**
- 13.1, 13.2, 13.3, 13.4, 13.9 — كلها تسأل «هل الخاصية موجودة في كل حالة من نطاق مُعلَن؟»

**البوابات التي تحتاج «ثبات» فقط (1):**
- 13.6 (perf) — الأداء ثبات على مدخل قياسي، لا تنوّع مدخلات.

**البوابات التي لا تُؤتمَت (1):**
- 13.5 (device compat) — الحلّ قائمة يدوية إلزامية قبل التسليم، ليست بديلاً وإنما حدّاً واقعياً.

---

## 14. بند جديد للمرحلة 4 — تحويل البوابات اليدوية إلى اختبارات دائمة

**يُضاف إلى `PHASES.md §المرحلة 4`** كبند تنفيذي:

```markdown
- ☐ **تحويل البوابات اليدوية إلى اختبارات دائمة** — سبعة سكربتات
      حارسة + قائمة يدوية واحدة (device compat). التفصيل في
      `PHASES-docs.md §13`. **قاعدة L-46:** كل سكربت يُصرِّح
      صراحةً هل يفحص وجود، ثبات، أو كليهما. البوابة القديمة في
      PHASES.md تُحدَّث بسطر «الحارس الدائم: <path>» بعد كل تنفيذ.
```

**قاعدة تنسيق مقترحة** (تُطبَّق أثناء التحويل، تُصبح لاحقاً قاعدة
عامة في `docs/11` أو ما يشبهه):

```
البوابة (متحقّقة YYYY-MM-DD):
1. ☑ <النصّ> — الحارس الدائم: `scripts/verify-<x>.mjs` — نوع: وجود
2. ☑ <النصّ> — الحارس الدائم: `packages/<pkg>/src/<x>.test.ts` — نوع: ثبات
3. ☑ <النصّ> — الحارس: ⚠ يدوي دوري (سبب: <سبب>) — نوع: <وجود/ثبات>
```

**الأثر على الوثيقة:** كل بوابة ☑ حالية في PHASES.md ستحتاج جولة
مسح لتصنيفها. المهمّة تُضاف إلى نفس البند أعلاه.

---

## سجل الدفعة الثالثة (2026-09-03)

**عولج في هذه الدفعة:**
- ✅ SVG رُقّي من 🔻 إلى ☐ في المرحلة 3.8 (PHASES.md + docs/12) وسطر الحالة الملخّص محدَّث
- ✅ 8 قواعد أُضيفت إلى CLAUDE.md (L-17 و L-33 في §بوابات التوقّف، L-36 في §سجل الأصول التسويقية، L-08 و L-14 و L-16 و L-22 و L-27 في §قواعد مستمدَّة من الدروس الجديد)
- ✅ 6 بنود دَين توثيق أُضيفت إلى PHASES.md كجدول DD-01..DD-06
- ✅ L-31 أُضيف إلى docs/12 §بروتوكول المسح (الجديد)
- ✅ 4 دروس تاريخية مُوسَمة في LESSONS.md (L-11, L-24, L-25, L-30) بـ«ملاحظة — لا قاعدة»
- ✅ L-46 (وجود ↔ ثبات) كُتب في LESSONS.md
- ✅ خطة تحويل البوابات (9 بنود) في PHASES-docs.md §13
- ✅ بند «تحويل البوابات اليدوية» صيغَ لإضافته إلى المرحلة 4 (نصّ جاهز في §14)

**تُرك للمالك:**
- إضافة بند «تحويل البوابات» رسمياً إلى PHASES.md §المرحلة 4 (لم أضعه لتفادي تضخّم Phase 4 قبل موافقة)
- تطبيق قاعدة تنسيق البوابات (§14) بأثر رجعي على البوابات ☑ القديمة (12 بوابة محروسة تحتاج تصنيف L-46 صريح)

---

# فحص أعمق — دفعة رابعة (2026-09-03)

**السياق:** المالك اعتمد الخطة وقرّر «البوابات قبل المنصة». الدفعة
الأولى (G1 · G2 · G4). §13 كان اكتشافاً؛ §15 أدناه هو **صياغة تنفيذية**
جاهزة للنسخ إلى main.

## 15. تذاكر البوابات — جاهزة للتنفيذ (G1..G6)

**قاعدة الاختيار:** ست تذاكر تُغطّي كل الفجوات التنفيذية. `G5` تحوَّل
إلى قائمة يدوية (device compat) لأنه لا يُؤتمَت. `G6` مؤجَّل للمرحلة 4
لكنّ معياره كُتب الآن. البوابات المُضافة لاحقاً (BiDi range extension،
attribution conditional) تدخل في G4 المُوسَّع أو تُضاف لاحقاً بلا اجتهاد.

**قاعدة النموذج:** كل تذكرة تحمل: اسم السكربت · النوع (L-46) · **ما
يفحصه بالضبط** بخطوات مرقَّمة · معيار النجاح (exit 0) · معيار الفشل
(exit 1) · التبعيات المعمارية · الجهد · الدفعة.

---

### 🟢 G1 — نظافة الأصول الأناضولية

**الوجهة:** `scripts/verify-clean-of-aa.mjs`
**النوع (L-46):** وجود (عبر كل ملفات المصدر × كل رمز محظور).
**الدفعة:** 1 — أولوية.

**ما يفحصه بالضبط:**
1. تشغيل `git grep -l "<TOKEN>"` لكل رمز في القائمة: `AA_LOGO`، `CV_LOGO`، `HelveticaNeueLTArabic`، `HNArabic`، `AA-60-SANIYE`، `aa_customLogo`.
2. استبعاد المسارات المسموح لها بذكر الرموز تاريخياً: `docs/INVENTORY.md`، `docs/LESSONS.md`، `docs/M1-marketing-assets.md` (في سياق التوثيق التاريخي).
3. طباعة قائمة المطابقات لكل رمز مع سطر الملف.

**معيار النجاح (exit 0):** صفر مطابقات في كل الملفات خارج القائمة البيضاء.

**معيار الفشل (exit 1):** أيّ مطابقة واحدة خارج القائمة البيضاء — يطبع الملف والسطر والرمز.

**التبعيات المعمارية:** لا شيء — أداة سطر أوامر خالصة (`git grep`).

**الجهد المقدَّر:** 30-45 دقيقة.

---

### 🟢 G2 — عزل الهوية والقالب عن كود المحرك

**الوجهة:** `scripts/verify-brand-template-isolation.mjs` (تُوحّد ما كان 13.2 + 13.3).
**النوع (L-46):** وجود (عبر كل brand/template جديد يُدخَل).
**الدفعة:** 1 — أولوية.

**ما يفحصه بالضبط:**
1. `git status --porcelain` للتأكد من نظافة الشجرة قبل الاختبار.
2. لكل ملف في `brands/*.json` **مضاف حديثاً في التزام واحد** (يكتشف عبر `git diff --diff-filter=A` بين HEAD وHEAD~1):
   - تشغيل `pnpm preview -- --brand=<name>` وتأكيد الخروج بـexit 0.
   - `git diff --exit-code -- packages/` — يجب أن يعود صفر تغيير.
3. لكل ملف في `packages/templates/src/templates/*.json` **مضاف حديثاً**:
   - تشغيل `pnpm preview -- --template=<name> --brand=default` وتأكيد الخروج بـexit 0.
   - `git diff --exit-code -- packages/engine` — يجب أن يعود صفر تغيير.
4. تشغيل `pnpm verify:snapshot` وتأكيد 24/24 مطابقة (أو العدد الحالي في ذلك الوقت).

**معيار النجاح (exit 0):** كل brand/template جديد أُضيف يُنتج مخرجاً بلا لمس `packages/engine/*` ولا `packages/templates/src/*.ts`.

**معيار الفشل (exit 1):** أيّ تغيير في `packages/` بعد إضافة brand/template — يطبع الفرق.

**التبعيات المعمارية:** لا شيء — يستعمل `pnpm preview` و `git diff` الحاليَّين.

**الجهد المقدَّر:** 2-3 ساعات (كتابة السكربت + اختبار على ملفَي client-demo و plain الموجودين كحالتَي دخول).

---

### 🟡 G3 — ميزانية أداء `buildRenderPlan`

**الوجهة:** `scripts/verify-render-plan-perf.mjs`
**النوع (L-46):** ثبات (نفس المدخل، ثبات الأداء عبر تشغيلات).
**الدفعة:** 2.

**تصحيح جوهري (توجيه المالك):** العتبة **تُشتق من القياس الحالي**، لا
من بيانات تاريخية. الطريقة:

1. عند أول تشغيل: يقيس الأداء الحالي على مدخل قياسي 100 مرة، يحسب p95،
   يكتب النتيجة في `scripts/perf-baseline.json` (مُلتزَم في git).
2. عند التشغيلات التالية: يقارن p95 الحالي بـ `baseline × 1.3` — يمرّ
   إن كان أدنى، يفشل إن كان أعلى.
3. **العتبة تُراجَع بعد شهر** (يُوثَّق تاريخ آخر تحديث في `perf-baseline.json`).

**ما يفحصه بالضبط:**
1. قراءة `scripts/perf-baseline.json` — إن غير موجود، اعتبره أول تشغيل، اقس، اكتب، exit 0.
2. تشغيل `buildRenderPlan` على مدخل قياسي (يُعرَّف في السكربت — نصّ عاجل 11 كلمة، `DEFAULT_BRAND`، breaking template) 100 مرة.
3. حساب p95 من زمن التشغيل.
4. مقارنة: `p95_current ≤ baseline_p95 × 1.3`.

**معيار النجاح (exit 0):** `p95_current ≤ baseline_p95 × 1.3` (أو أول تشغيل — يُنشئ baseline).

**معيار الفشل (exit 1):** `p95_current > baseline_p95 × 1.3` — يطبع الفرق ونسبته.

**التبعيات المعمارية:** لا شيء — `buildRenderPlan` موجود ومصدَّر.

**الجهد المقدَّر:** 1-2 ساعة.

---

### 🟡 G4 — ثبات الكشيدة عبر أنماط highlight في `caption`

**الوجهة:** `scripts/verify-caption-kashida-stability.mjs`
**النوع (L-46):** **كلاهما** — وجود عبر 5 أنماط highlight + ثبات على استدعاءات متكرِّرة لكل نمط.
**الدفعة:** 1 — أولوية (المُحفِّز اليوم).

**قرار معماري — بديل أقل تدخّلاً:**

المسار الأصلي المقترح كان تعديل توقيع `drawWord` لاستقبال callback تسجيل
— يعدّل واجهة عامة في `packages/engine`. **البديل الأنظف:**

- إضافة حقل اختياري في input للـ`renderFrame` أو `drawTimelineAt`:
  ```ts
  debug?: {
    captureKashidaPlacements?: boolean;
  }
  ```
- إن كان `true`، `drawWord` (داخلياً) يكتب مواضع التطويل في `state.debug.kashidaPlacements[]` بدل تعديل توقيعه.
- المخرج يُقرَأ من الـ state المُعاد لا من hook.

**فائدة البديل:** صفر تغيير في توقيع أيّ دالة عامة. حقل واحد إضافي
في نوع `RenderState` (اختياري). الاختبار يمرّر `debug: { captureKashidaPlacements: true }` ويقرأ من `state.debug.kashidaPlacements`.

**القرار النهائي لـmain** — أيّ المسارين أنظف بحسب البنية الفعلية
لـ`caption.ts` الحالي. **أنا فرع توثيق، أقترح ولا أحسم.**

**ما يفحصه بالضبط:**
1. تعريف مدخل قياسي: نصّ عاجل عربي 11 كلمة، `DEFAULT_BRAND`، مقاس 1080×1920 (ريلز).
2. لكل نمط من أنماط highlight الخمسة (word-fade · word-slide · box-tight · box-soft · underline — الأسماء الفعلية تُقرأ من `caption.ts`):
   - رندر إطار عند t=2.0 (منتصف الكابشن) خمس مرات متتالية.
   - جمع `kashidaPlacements` من الـ state أو الـ callback.
   - **ثبات (داخل النمط):** الاستدعاءات الخمسة تُنتج نفس المواضع بالضبط (deep equal).
3. **وجود (عبر الأنماط):** المواضع في الأنماط الخمسة **متطابقة كلياً** — لأن الكشيدة خاصية النصّ لا خاصية الإظهار.

**معيار النجاح (exit 0):**
- ثبات: 5 × 5 = 25 استدعاءً، كلها متطابقة داخل كل نمط.
- وجود: 5 أنماط، كلها تُنتج نفس مواضع الكشيدة (بايت-بايت في التمثيل JSON).

**معيار الفشل (exit 1):**
- أيّ اختلاف بين استدعاءات نفس النمط → **حالة تسرَّب** (طباعة الأنماط المتناقضة).
- أيّ اختلاف بين نمطين → **الوجود مكسور** — الكشيدة تتأثّر بنمط الإظهار (طباعة النمطَين والفرق).

**التبعيات المعمارية:**
- إضافة `debug.captureKashidaPlacements` (بديل أقل تدخّلاً — لا يعدّل توقيعات).
- **أو** hook في `drawWord` (المسار الأصلي — يعدّل التوقيع، مرفوض إن أمكن الأول).

**الجهد المقدَّر:** 2-3 ساعات (اختيار المسار + كتابة الاختبار).

---

### 🟠 G5 — قائمة يدوية إلزامية: التوافق مع أجهزة الاستهلاك

**الوجهة:** `docs/09-launch-spec.md §القبول قبل التسليم` (بند جديد).
**النوع (L-46):** وجود يدوي — لا يُؤتمَت.
**الدفعة:** 2 — قبل أول تسليم لعميل.

**السبب في عدم الأتمتة:** يحتاج مزرعة أجهزة (iOS/Android × عدة إصدارات
+ حساب Instagram اختبار). الكلفة أعلى من قيمة الأتمتة لثلاثة عملاء
أوائل. القائمة اليدوية كافية.

**ما تحمله القائمة (تُلصق في `docs/09`):**

```markdown
## القبول قبل التسليم (يُنفَّذ لكل عميل جديد)

- ☐ MP4 عاجل يفتح على iPhone (Safari + التطبيقات الأصلية)
- ☐ MP4 عاجل يفتح على أندرويد (Chrome + Gallery)
- ☐ MP4 يُرفَع إلى Instagram Feed بلا إعادة ترميز مرئية
- ☐ MP4 يُرفَع إلى Instagram Reels بلا قصّ غير مقصود
- ☐ الصوت مسموع على iOS و Android (تحقّق سماعات + مكبِّرات)
- ☐ الألوان لا تنحرف على iPhone (color primaries bt709)
```

**معيار النجاح:** كل ست خانات ☑ قبل إعلان جاهزية العميل.

**معيار الفشل:** خانة واحدة ☐ = تسليم غير جاهز.

**التبعيات المعمارية:** لا شيء — تحديث وثيقة.

**الجهد المقدَّر:** 15 دقيقة (تعديل docs/09) — العمل الفعلي مع كل عميل.

---

### ⚪ G6 — عزل المستأجرين على مستوى قاعدة البيانات (Phase 4)

**الوجهة:** `scripts/verify-tenant-isolation.mjs` (يُنفَّذ في المرحلة 4).
**النوع (L-46):** كلاهما — وجود (كل نقطة قراءة/كتابة معزولة) + ثبات (RLS يعمل باستمرار).
**الدفعة:** 3 — مؤجَّل للمرحلة 4، **لكن المعيار مكتوب الآن** حسب توجيه المالك.

**ما يفحصه بالضبط (عند البناء في المرحلة 4):**

1. **إعداد بيئة اختبار:** إنشاء مستأجرَين وهميَّين (`tenant_A`, `tenant_B`) مع سجلات في كل جدول تحت RLS: `brand_kits`, `projects`, `renders`, `revisions`, `annotations`.
2. **اختبار الوجود (كل نقطة معزولة):**
   - لكل جدول تحت RLS، جلسة PostgreSQL بـ`SET LOCAL app.tenant_id = 'tenant_A'`.
   - `SELECT * FROM <table>` — يجب أن يعيد سجلات tenant_A فقط.
   - `INSERT INTO <table> (tenant_id, ...) VALUES ('tenant_B', ...)` — يجب أن يفشل (violates RLS).
   - `UPDATE <table> SET ... WHERE tenant_id = 'tenant_B'` — يجب أن يعيد 0 rows affected.
3. **اختبار الثبات (RLS مفعَّل باستمرار):**
   - لكل جدول تحت RLS، `SELECT relrowsecurity, relforcerowsecurity FROM pg_class WHERE oid = '<table>'::regclass` — يجب أن يكون كلاهما `true`.
   - عدد سياسات RLS لكل جدول ≥ 1 (`SELECT count(*) FROM pg_policies WHERE tablename = '<table>'`).
4. **اختبار التسرّب عبر الأخطاء:** رسائل الخطأ التي يعود بها الخادم لا تكشف بيانات المستأجر الآخر (رسالة عامة «entity not found» لا «entity of tenant_X not found»).

**معيار النجاح (exit 0):**
- كل SELECT/INSERT/UPDATE يحترم RLS (10 من 10 حالات في كل جدول).
- كل الجداول تحت `relrowsecurity = true` و `relforcerowsecurity = true`.
- عدد سياسات RLS ≥ 1 لكل جدول محمي.
- رسائل الخطأ لا تسرِّب معرِّفات مستأجر آخر.

**معيار الفشل (exit 1):**
- أيّ استعلام يعود ببيانات مستأجر آخر → **تسرّب** (طباعة السجل والاستعلام).
- جدول بلا RLS → **حماية غائبة** (طباعة اسم الجدول).
- رسالة خطأ تكشف معرِّف مستأجر آخر → **تسرّب معلومات** (طباعة الرسالة).

**التبعيات المعمارية:**
- PostgreSQL مثبَّت مع بذرة اختبار.
- سياسات RLS مكتوبة لكل جدول (بند مستقلّ في المرحلة 4).

**الجهد المقدَّر:** 4-6 ساعات (إعداد بيئة + كتابة الاختبار).

**سبب كتابة المعيار الآن:** السياق حاضر — سجل المراجعات (docs/14)
وسير التحرير (docs/15) وحقول `brand.attribution.logoAcks` كلها تحت
حماية RLS. لو تُرك المعيار للمرحلة 4، سيُخترع لاحقاً بلا هذه الخيوط.

---

## 16. الجدول المُوجز للتنفيذ في main

| # | الاسم | الدفعة | الوجهة | النوع L-46 | جهد | تبعيات معمارية |
|---|---|---|---|---|---|---|
| G1 | نظافة الأصول الأناضولية | **1** | `verify-clean-of-aa.mjs` | وجود | 30-45د | لا شيء |
| G2 | عزل الهوية والقالب | **1** | `verify-brand-template-isolation.mjs` | وجود | 2-3س | لا شيء |
| G3 | ميزانية أداء RenderPlan | 2 | `verify-render-plan-perf.mjs` + `perf-baseline.json` | ثبات | 1-2س | لا شيء |
| G4 | ثبات كشيدة caption عبر modes | **1** | `verify-caption-kashida-stability.mjs` | كلاهما | 2-3س | حقل `debug.captureKashidaPlacements` (بديل أقل تدخّلاً) |
| G5 | توافق أجهزة الاستهلاك | 2 | تعديل `docs/09` | يدوي | 15د | لا شيء (يدوي) |
| G6 | عزل المستأجرين (RLS) | 3 | `verify-tenant-isolation.mjs` | كلاهما | 4-6س | Phase 4 (RLS مكتوب) |

**الدفعة الأولى (اليوم/غداً):** G1 + G2 + G4 = **5-7 ساعات إجمالاً**.
**الدفعة الثانية (بعد اكتمال الأولى):** G3 + G5 = **1-2 ساعة + دقيقة تعديل**.
**الدفعة الثالثة (المرحلة 4):** G6 = **4-6 ساعات** ضمن بناء المنصة.

---

## سجل الدفعة الرابعة (2026-09-03)

**عولج:**
- ✅ 6 تذاكر جاهزة (G1..G6) في §15 — كل واحدة قابلة للنسخ إلى main بلا اجتهاد
- ✅ G4 يحمل بديلاً أقل تدخّلاً (`debug.captureKashidaPlacements` بدل تعديل `drawWord`) — القرار لـmain
- ✅ G3 يستعمل قياساً حالياً × 1.3 لا بيانات تاريخية — `perf-baseline.json` كملف موزون
- ✅ G6 معيار مكتوب الآن (كلاهما — L-46) رغم تأجيل التنفيذ إلى المرحلة 4
- ✅ جدول تنفيذي مُوجز في §16

**لم يُصحَّح شيء في scripts/** — فرع توثيق، الخطة تنتقل إلى main.

---

# ما يُسلَّم إلى main

> **قسم ختامي.** كل ما بقي قابلاً للتنفيذ بعد دمج هذا الفرع، مرتَّباً
> بالأولوية. القسم الأوّل ينفَّذ فوراً بلا اجتهاد؛ الثاني ينتظر قرار
> المالك؛ الثالث تراكم للجولات القادمة.

## أ. قابل للتنفيذ فوراً في main (بلا قرار)

| # | البند | الملف المستهدف | التغيير | يلمس packages/؟ |
|---|---|---|---|---|
| A1 | **G1 نظافة الأصول الأناضولية** — التذكرة الكاملة في §15 | `scripts/verify-clean-of-aa.mjs` (جديد) | كتابة سكربت `git grep` (30-45د) | لا |
| A2 | **G2 عزل الهوية والقالب** — التذكرة في §15 | `scripts/verify-brand-template-isolation.mjs` (جديد) | كتابة سكربت `preview + git diff --exit-code` (2-3س) | لا |
| A3 | **G3 ميزانية أداء RenderPlan** — التذكرة في §15 (عتبة من قياس × 1.3) | `scripts/verify-render-plan-perf.mjs` + `scripts/perf-baseline.json` | سكربت قياس + baseline موزون (1-2س) | لا (يستهلك `buildRenderPlan` كما هي) |
| A4 | **G5 قائمة يدوية للتوافق الجهازي** — نصّ جاهز في §15 | `docs/09-launch-spec.md` (بند جديد §القبول قبل التسليم) | نسخ ست خانات ☐ (15د) | لا |
| A5 | **DD-01..DD-06 دَين توثيق** — سطور محدَّدة في §15 و PHASES.md | `docs/08` · `docs/11` · تعليق في `apps/renderer/src/queues.ts` · `apps/dashboard` (لاحقاً) | ست إضافات نصّية (ساعة تراكمياً) | تعليق في `apps/renderer` (سطر واحد) — بقيّتها docs |
| A6 | **بند «تحويل البوابات» رسمياً في المرحلة 4** — النصّ جاهز في §14 | `PHASES.md §المرحلة 4` | نسخ فقرة تحت §الأعمدة الأساسية | لا |
| A7 | **قاعدة تنسيق البوابات** — الشكل المقترح في §14 | `PHASES.md` (سطر توجيهي بعد §نظرة عامة أو في §المرحلة 4) | إضافة فقرة قصيرة | لا |
| A8 | **مزامنة DD-06** — L-15 كسطر في «قاعدة البوابات» | `PHASES.md §نظرة عامة` | سطر واحد يُضاف | لا |

**إجمالي الجهد لهذه المجموعة:** 6-8 ساعات كود · 20 دقيقة تحرير docs.

---

## ب. يحتاج قرار المالك قبل التنفيذ

| # | البند | القرار المطلوب | مرجع |
|---|---|---|---|
| B1 | **L-46 المزدوج** — نسختان بنفس الرقم في LESSONS.md | اختر: (1) إبقاء main، (2) دمج الاثنين، (3) إبقاء main + ترقية مسودتي إلى L-47. **توصيتي: الدمج.** | LESSONS.md سطر 1334 مقابل 1379 |
| B2 | **G4 مقارنة بـcommit `e2d730a` على main** | main بنى «four-gate kashida verification» ذاتياً — قارن بتذكرة G4 في §15، صحّح لو لزم. **قد يكون G4 مُنجَزاً كلياً أو جزئياً.** | `git show e2d730a` |
| B3 | **ADR-010 اعتماد النصّ** — مسوّدة كاملة في §11 | إدراج في `docs/02-architecture.md §ADR` بعد المراجعة | مسوّدة §11 |
| B4 | **ADR-011 (LRI/PDI) و ADR-012 (AudioPlan)** — اقتراحات تعليقات كوديّة تستحق ترقية | كتابة نصّ (اقتراح فقط في §11) | تعليقات في `attribution.ts` و `audio-graph.ts` |
| B5 | **1.4 عدد قواعد PROJECT_INSTRUCTIONS.md** — 9 مقابل 10 في CLAUDE.md | إضافة القاعدة 10 أو الإحالة إلى CLAUDE.md | `docs/PROJECT_INSTRUCTIONS.md` |
| B6 | **1.5 عدد الدروس في CLAUDE.md** — «6 دروس» غير محدَّث (الفعلي ~46) | حذف الرقم أو تحديثه (سطر 106) | `CLAUDE.md:106` |
| B7 | **1.8/1.9 عدد القوالب** — «أربعة» (09) مقابل «ستة» (06/07/الفعلي) | قرار تسويقي: أيّ الرقمين يظهر في المادة العامة | `docs/04:46` · `docs/09:52` · `docs/06:29,56` |
| B8 | **1.12 SLA عاجل** — 45s (06/CLAUDE) · 60s (08) · دقيقتان (08) | حسم رقم واحد أو إعادة قياس | `docs/06:79` · `docs/08:152-154` · CLAUDE.md |
| B9 | **10.3 كشف الوجوه** — لا مُشغِّل | تحديد ما يُشغّل بدء العمل | `PHASES.md:711` |
| B10 | **10.11-10.13 restart/kill/webhook** — أبواب مغلقة على main | فك القفل + اختيار cron أو BullMQ repeat لـwebhook | `PHASES.md:420, 422, 423-424` |

---

## ج. تراكم للجولات القادمة (لا يحجب الإصدار)

| # | المحور | العدد | مرجع |
|---|---|---|---|
| C1 | **الفجوات المعمارية** — قرارات كود بلا سطر توثيق | 22 | §5 |
| C2 | **مواصفة ↔ كود** — brandKit + templates | 30+ | §6 (الأخطر: جدول docs/04 قديم كلياً) |
| C3 | **دروس بلا تطبيق كامل** — B (6) في وثائقها الطبيعية | 6 | §7 (L-18, L-19, L-20, L-21, L-23 + L-15) |
| C4 | **بنود مؤجَّلة بشرط ضبابي** — من §10 (بعد استبعاد ما نُقض) | 10 | §10-ب |
| C5 | **أرقام بلا مصدر** — 11 من §2 | 11 | §2 (2.2..2.12) |
| C6 | **نقل الدروس التاريخية** — L-11, L-24, L-25, L-30 إلى `docs/LESSONS-archive.md` | 4 | LESSONS.md |
| C7 | **قاعدة L-46 على البوابات القديمة** — تصنيف 12 بوابة محروسة | 12 | §14 |

---

## دليل استخدام هذا الملف بعد الدمج

**عند فتح جلسة main تالية:**

1. اقرأ **قسم أ** — يحدّد ما يمكن أن تفعله بلا استشارة.
2. اقرأ **قسم ب** — تعرّف القرارات المعلَّقة قبل لمس البنود المرتبطة.
3. **§13 و §15** خطة البوابات الأصلية والمنقّحة — استعملها كمرجع تنفيذ.
4. **§9** تصنيف الدروس — قبل ترقية أيّ درس إلى قاعدة، راجع التصنيف.
5. **قسم ج** يُراجَع دورياً (بحسب L-27) — البنود قد تنتقل إلى (أ) أو (ب) بحجّة.

**قاعدة L-40:** كل جولة تحرير على هذا الملف تُفتح على فرع
`docs/<موضوع>` مستقل، لا داخل مسار الكود.

---

**نهاية الجلسة.** ما لم يُذكر في (أ) أو (ب) أو (ج) فهو منجَز أو
مُغلَق أو خارج نطاق مسؤولية `docs/marketing`.
