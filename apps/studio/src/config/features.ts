// features — أعلام ميزات الاستوديو. علم واحد لكل ميزة قابلة للإخفاء
// وراء قرار المالك، بلا حذف كود ولا حذف خدمة.
//
// **قاعدة:** اسم واضح، قيمة ثابتة boolean، تعليق يوضّح السياق. يوم
// نقرّر إعادة الإظهار: سطر واحد.

/** TASHKIL-UI-OFF (2026-09-10): يخفي عناصر واجهة التشكيل في مساحة
 * المشروع (زرّ «شكّل»، زرّ «جرد»، لوحة إدخال التشكيل الافتراضيّة).
 * الخدمة والمسار (/api/diacritize و services/diacritizer:19080) يبقيان
 * كما هما — يُطوَّران في الخلفيّة. النصوص المُشكَّلة مسبقاً (لصق من
 * خارج) تمرّ إلى المحرك كما هي.
 *
 * لإعادة الإظهار: اجعل هذه القيمة `true`. لا تغيير آخر مطلوب. */
export const TASHKEEL_UI_ENABLED = false;
