# demo — لقطات مقارنة قبل/بعد الكسر الدلالي

العناوين مختارة من مسح 265 عنوان RSS. كل واحد يُظهر أثر قاعدة
مختلفة — ما لا تفعله Canva ولا أي أداة تصميم عربية.

التمييز البصري بين الملفَين: `-before.png` = enabled=false،
`-after.png` = enabled=true + ExtendedLexicon (places · entities · titles).

## demo 1 — particle

**العنوان الكامل:**
> الاتحاد الأوروبي يحذّر صربيا من تكريم مجرم الحرب ملاديتش

- **المصدر:** aawsat
- **القاعدة التي تدخّلت:** حرف جر (BREAK_INFINITY)
- **السبب:** الكسر بعد «من» — حرف جر ملازم لمجروره — يُمنع دلالياً.

**قبل** (fs=80, بلا الكسر الدلالي):
```
الاتـــحاد الـــأوروبي يحذّر صربـــيا من
تكريم مجرم الحرب ملاديتش
```

**بعد** (fs=80, مع الكسر الدلالي):
```
الاتـــحاد الـــأوروبي يحذّر صربـــيا
من تكريم مجرم الحرب ملاديتش
```

**الملفَان:** `demo/demo-1-before.png` · `demo/demo-1-after.png`

---

## demo 2 — place

**العنوان الكامل:**
> سوريا.. بدء ضخ الغاز من حقل كونيكو في دير الزور لدعم قطاع الكهرباء

- **المصدر:** aljazeera
- **القاعدة التي تدخّلت:** اسم مركّب في places.json (BREAK_STRONG)
- **السبب:** «دير الزور» زوج معروف من GeoNames — لا يُفصل بين طوكنَيه.

**قبل** (fs=80, بلا الكسر الدلالي):
```
سوريـــا.. بـــدء ضخ الغـــاز
من حـــقل كونـــيكو في دير
الزور لدعم قطاع الكهرباء
```

**بعد** (fs=80, مع الكسر الدلالي):
```
سوريـــا.. بـــدء ضخ الغـــاز
من حــقل كونــيكو في دير الـزور
لدعم قطاع الكهرباء
```

**الملفَان:** `demo/demo-2-before.png` · `demo/demo-2-after.png`

---

## demo 3 — title

**العنوان الكامل:**
> تجديد عقد حسام حسن مدرب منتخب مصر حتى 2030

- **المصدر:** aawsat
- **القاعدة التي تدخّلت:** لقب في titles.json (BREAK_STRONG)
- **السبب:** «مدرب» لقب مهني — رابطة قوية مع الاسم/المنصب الذي يليه.

**قبل** (fs=80, بلا الكسر الدلالي):
```
تجــديد عـقد حسـام حـسن مـدرب
منتخب مصر حتى 2030
```

**بعد** (fs=80, مع الكسر الدلالي):
```
تجـــديد عـــقد حســـام حـــسن
مدرب منتخب مصر حتى 2030
```

**الملفَان:** `demo/demo-3-before.png` · `demo/demo-3-after.png`

---

## مخرجات إضافية من المرحلة 3.8/3.10 (2026-09-04)

منقولة من `out/` بعد إثبات صلاحها للعرض (L-48). كل ملفّ يُظهر ميّزة
مذكورة في M1 أو PHASES.

### attribution-demo.png · attribution-demo-bidi.png

طبقة الإسناد (attribution) — 18 خلية × 3 أوضاع شعار (none/generic/
official) × هويّتين + BiDi للمقبض اللاتيني داخل عربي (LRI/PDI).
مسجَّلة في **M1 §د-1**. الفيكستشر: 6 منصات مدعومة (tiktok · x ·
instagram · youtube · telegram · facebook).

### placement-demo.png

نظام المواضع الموحّد (`brand.placement`) — 9 أنكورات × عناصر متعدّدة
(logo · badge · attribution · source · caption). دليل أن أيّ عنصر
يُموضَع بنفس المفردات في كل قالب.

### svg-demo.png (1200×700)

طبقة SVG بمحرّكنا — subset مدعوم (path · rect · circle · ellipse ·
line · polyline · polygon · g/defs · transform · clipPath) مع ربط
الألوان بالهوية (`data-brand-fill="accent"`). الشبكة تعرض هويّتين
مختلفتين تُنتِجان نفس الفيكستشر بألوان مختلفة. مسجَّلة في **PHASES
§المرحلة 3.8 · SVG**.

### smart-crop-demo.png (1360×1358)

القصّ الذكي — شبكة 4 مقاسات × مركزي/ذكي. الذكي يزيح الإطار نحو
«الوجه» ويحوّل مسافة المركز من 607px إلى 201px (×3 تحسّن). مسجَّلة
في **PHASES §المرحلة 3.8 · كشف الوجوه**.

### multilang-demo.png (1740×840)

`content.locale` — ثلاث لغات (ar · en · tr) في نفس القالب. العربية
بكشيدة، اللاتينية بلف بسيط، كلها بمحاذاة بصرية موحّدة بعد إصلاح
L-50 (pixel-based ascent). مسجَّلة في **PHASES §المرحلة 3.10**.

### caption-modes-demo.mp4 (35s)

طبقة caption على المحرك — 5 أنماط تلوين متتابعة (wordColor ·
wordBackground · progressiveReveal · wordScale · none). الكشيدة
مستقرّة عبر الأنماط الخمسة (md5 موحّد). مسجَّلة في **M1 §ب-4**.

### tts-demo.mp4 (10s)

TTS Gateway مع mock adapter — موسيقى 220Hz + تعليق صوتي + ducking
تلقائي عبر `sidechaincompress`. المفتاح لا يظهر في المخرج (redactKey).
مسجَّلة في **PHASES §المرحلة 3.8 · التعليق الصوتي**.

---

## قاعدة الحفظ (L-48)

هذه الملفات مُتَتبَّعة في git لأن الوثائق تُحيل إليها. أيّ حذف يجب
أن يسبقه حذف الإحالة من M1/M2 (يفشل `pnpm check:doc-paths` إن ورد
`out/` في وثيقة تسويقية). الملفات المؤقّتة تبقى في `out/`
(المستثنى من Git).

---

## demo/studio — لقطات مراجعة الواجهة

**مادة مراجعة بشرية، لا مرجع تحقّق آلي.** الفرق عن `snapshots*/`
(التي تُقارَن بـmd5 وتفشل عند بايت واحد يختلف): لقطات الواجهة تتغيّر
مع كل تعديل تصميم — مقارنة بايتية عليها تعني فشلاً دائماً، والفحص
الذي يفشل دائماً يُعطَّل بعد أسبوع (L-55). هذه اللقطات لعين المالك
عند مراجعة أيّ تسليم يمسّ الواجهة (L-17). ملتزَمة في git لأن
`demo/` هي مكان ما يُعرض (L-48).

**نمط الأرشفة عبر المراحل:** كل مرحلة تُبقي لقطاتها السابقة كطبقات —
`projects.png` (S1 بمفاتيح خام) لا يُستبدل بـ`projects-ar.png` (S4
بترجمة). الجديد يُضاف، القديم يبقى كسجل مرئي لتطوّر الواجهة.

### الإطار (S1 · `cadbb28`)

- `projects.png` — أوّل التقاط للتخطيط الرئيسي. مفاتيح i18n خام
  (`pages.projects.title`, `nav.projects` …) قبل ملء القواميس.
  الغاية: توثيق أن الإطار يعمل قبل i18n.
- `login.png` — أوّل التقاط لشاشة المصادقة، بلا i18n بعد.

### i18n ثلاث لغات (S4 من `docs/17` · `93fa429`)

- `projects-ar.png` — RTL كامل، الشريط الجانبي على اليمين، نصوص
  عربية مترجمة من `pages.projects.*`.
- `projects-mixed.png` — RTL، وحدات مستقلّة إنجليزية (Projects,
  Brand Kits …) + جمل توصيفية عربية. L-24 مطبَّق.
- `projects-en.png` — LTR، كل النصوص إنجليزية، الشريط الجانبي
  إلى اليسار.
- `login-ar.png` — بطاقة مصادقة مركزية، شارة `MEDIA KIT` ذهبية،
  عناوين عربية، حقول dir=ltr للبريد وكلمة السر، زرّ primary ذهبي.
- `login-mixed.png` — نفس البطاقة، عناوين إنجليزية على unit-level،
  رسائل عربية.
- `login-en.png` — LTR كامل.

### نظام التصميم (S2 · `fe44d4f`)

- `design-ar.png` — كل atom في مكان واحد: `Button` بـ4 variants ×
  2 sizes + loading + disabled · `Input` · `Textarea` · `Field` ·
  `Card` · `Table` · `Dialog` · `Alert` بـ4 أنواع · `Badge`. RTL.
- `design-mixed.png` — نفس المحتوى، mixed. عناوين البطاقات إنجليزية.
- `design-en.png` — نفس المحتوى، LTR كامل.

### الأرقام والاتجاه (امتداد خارج ترقيم `docs/17` · `ef98a25`)

- `design-ar-latin.png` — RTL + أرقام لاتينية. بطاقة «Numbers &
  Direction» تُظهر `460.4 GB / 108.0 GB` (صحيح مع Ltr)،
  `2026-09-04, 14:23`، «منذ 7 دقيقة».
- `design-ar-arab.png` — RTL + أرقام هندية. تصبح `٤٦٠٫٤ GB /
  ١٠٨٫٠ GB`، `٢٠٢٦-٠٩-٠٤, ١٤:٢٣`. الوحدة `GB` تبقى لاتينية (SI).
- `design-en-latin.png` — LTR + لاتينية.

**سطر يُكتب بسببه — العيّنة L-23 counter-example:** بطاقة «Usage»
داخل `design-*` تعرض `42 / 100` **بلا `<Ltr>`**، فتظهر معكوسة
`100 / 42` في اتجاه RTL بجوار بطاقة «Storage» الصحيحة (المغلَّفة
بـLtr). **تبقى المعكوسة في المعرض عمداً** — الغاية أن يرى المطوّر
العيب مقروناً بحلّه، لا شرحاً نظرياً عن `dir="ltr"`.

### استخراج packages/ui + packages/i18n (S2-X · `f70d202`)

- `design-ar-post-extract.png` — نفس صفحة `/design` بعد نقل الذرّات
  إلى `packages/ui` واعتماد `tailwind-preset` مشترك + `tokens.css`.
  الغاية: إثبات أن الاستخراج لم يكسر الرندر — الألوان، الخطوط،
  الهوامش، الوحدات — بقيت مطابقة.

**سطر يُكتب بسببه — حجم الملف مختلف عن `design-ar.png` (115,028
مقابل 86,083 بايت):** الفرق كامله من ارتفاع الالتقاط
(`--window-size=1440,1700` للجديد مقابل `1440,900` للأصل)، لا فرق
رندر ولا فرق تصميم. المحتوى الظاهر في الـviewport متطابق.

### صفحات المصادقة على mocks (S5 · `e32ca0e`)

مبنيّة على `packages/ui` + `packages/i18n`، مربوطة بـ`src/api/mock.ts`
عبر `NEXT_PUBLIC_API_MOCK=true`. لا استدعاء HTTP فعلي — SYNC-α لم
تُفتح.

- `s5-login-ar.png` — تسجيل الدخول، AR. الحقول فارغة، الزرّ primary.
- `s5-login-mixed.png` — نفس الشاشة، mixed.
- `s5-login-en.png` — نفس الشاشة، EN.
- `s5-signup-ar.png` — إنشاء الحساب (اسم الوكالة + بريد + كلمة سر
  جديدة)، AR. تلميح «12 حرفاً على الأقل» تحت حقل كلمة السر.
- `s5-signup-mixed.png` — نفس الشاشة، mixed.
- `s5-signup-en.png` — نفس الشاشة، EN. «At least 12 characters» hint.
- `s5-forgot-password-ar.png` — استعادة كلمة السر (حقل بريد فقط)، AR.
- `s5-forgot-password-mixed.png` — نفس الشاشة، mixed.
- `s5-forgot-password-en.png` — نفس الشاشة، EN.
- `s5-reset-password-ar.png` — تعيين كلمة سر جديدة (يقرأ `?token=…`
  من URL)، AR.
- `s5-reset-password-mixed.png` — نفس الشاشة، mixed.
- `s5-reset-password-en.png` — نفس الشاشة، EN.

**سطر يُكتب بسببه — `s5-login-ar-error-401.png`، مسار فكّ الخطأ
كاملاً:**

1. المستخدم أدخل `email=user@example.com` + `password=wrongwrongwrong`
   (يتخطّى client-validate — البريد صحيح الشكل، كلمة السر 12 حرفاً+).
2. `AuthCard.onSubmit` استدعى `auth.login(...)` → `request()` →
   `handleMock('POST', '/v1/auth/login', body)`.
3. الـmock طبَّق قاعدة «لا كلمة سر تُقبل إلا `letmein12345`» → رمى
   `ApiError({ code: 'INVALID_CREDENTIALS', messageKey:
   'errors.INVALID_CREDENTIALS', status: 401 })`.
4. `AuthCard` التقط الاستثناء، فحص `err.field` (null) → عرض
   `<Alert kind="danger" titleKey={err.messageKey} />`.
5. `Alert` استدعى `useLocale().t('errors.INVALID_CREDENTIALS')` →
   قرأ من `packages/i18n/src/ar.json` القيمة «بريد أو كلمة سر خاطئة.».
6. الشريط الأحمر أعلى النموذج ظهر بالنصّ المفكوك.

هذه اللقطة **تُثبت L-22 طرفاً إلى طرف** — رسائل الخادم مفاتيح، لا
نصوصاً. الترجمة تحدث في الواجهة عبر المفتاح الوارد من الـerror body.

### الربط الحقيقي بـmk-api (S6 · `536d661`)

أوّل تسليم يستدعي مصدراً حيّاً على `127.0.0.1:19040`. لُطِّم في ست
انحرافات معلَنة (docs/16 مقابل السلوك الفعلي) — لقطاته توثّق الحالة
المكسورة عمداً كي يقاس التقدّم لاحقاً.

- `s6-login-real-success.png` — دخول صحيح ضدّ mk-api حقيقي. اسم
  المستأجر «Studio Demo Agency» يظهر في الرأس بعد استدعاء تكميلي
  `GET /v1/tenant` (لأن login response لم يكن يحمل tenant.name
  حينها). بريد المستخدم يظهر placeholder «الحساب» لأن `login.user`
  الحقيقي كان `{id, role}` فقط — انحراف #4 من التقرير.
- `s6-login-real-401.png` — كلمة سر خاطئة ضدّ mk-api. البانر الأحمر
  يعرض «INVALID_CREDENTIALS» **خاماً بلا ترجمة**، لأن `error.message`
  حينها كان `"INVALID_CREDENTIALS"` (خرق L-22). انحراف #6.

**سطر يُكتب بسببه — `s6-refresh-evidence.png`، أوّل عطل كشفته SYNC-α:**
بعد إفساد access token وإعادة التحميل، `client.ts` استدعى refresh،
لكن mk-api أعاد `{session:{accessToken,…}}` مغلَّفاً بينما docs/16
§2.3 و client.ts يتوقّعان الجسم مفروشاً. النتيجة: أوّل ريفريش يعود
undefined، ثم المستخدم يُرمى إلى `/login`. اللقطة **تبقى في المعرض
عمداً** لأنها الدليل التنفيذي على أول انحراف اكتشفناه بعد فتح
SYNC-α — قبل أن يصلح mk-api الجسم في `410cc33`.

### تصحيح الانحرافات من الخادم (S6-FIX · `1534960`)

بعد A8-FIX من mk-api: refresh مفروش، login.tenant كامل بـname/plan،
`error.message` صار مفتاح i18n جاهزاً (`errors.CODE`).

**سطر يُكتب بسببه — `s6-login-real-401.png` مقابل
`s6fix-login-real-401-translated.png`:**
اللقطتان نفس الشاشة (login بـkey خاطئ ضدّ mk-api حقيقي). الفرق كلّه
من طرف الخادم:
- **قبل (`s6-login-real-401.png`):** البانر يعرض `INVALID_CREDENTIALS`
  حرفياً — الرمز الخام. `t('INVALID_CREDENTIALS')` لم يجد المفتاح
  فأعاده كما هو.
- **بعد (`s6fix-login-real-401-translated.png`):** البانر يعرض «بريد
  أو كلمة سر خاطئة.» — النصّ العربي الكامل. `error.message` صار
  `errors.INVALID_CREDENTIALS` (مفتاح i18n)، فتحوّل عبر القاموس.

**اللقطتان تُثبتان L-22 قبلاً وبعداً** — نفس رمز الواجهة، سلوك مختلف
لأن الخادم يرسل مفتاحاً لا رمزاً خاماً.

- `s6fix-login-real-success.png` — نجاح دخول حقيقي، اسم المستأجر
  «Studio Demo Agency» يظهر **فوراً** من login response (لا GET
  /v1/tenant تكميلي — بعد A8-FIX لم يعد لازماً).
- `s6fix-refresh-evidence.png` — بعد إفساد access token + reload،
  المستخدم **يبقى على `/projects`** — refresh نجح كاملاً. عكس
  `s6-refresh-evidence.png` بالضبط: نفس التجربة، خادم مختلف.

### منتقي الأصول (S8 · `a7f2322`)

رفع مباشر إلى signed URL (S3 عبر MinIO في dev)، منفصل عن طبقة
client.ts. تحقّق حجم قبل الرفع + مسار SVG-with-text warning.

- `s8-upload-real-success.png` — رفع `demo-avatar.png` (4 KB PNG) عبر
  mk-api + MinIO حقيقيَّين. الرفع نجح خادم-جانب (mk-api log +
  curl يؤكدان)، لكن UI list يعرض banner NETWORK_ERROR بسبب انحراف #7
  حينها (list response كان `{items,…}` لا `{data,…}`). أُصلح لاحقاً
  في A11-SHAPE — الشاشة الحالية تعرض القائمة صحيحة.
- `s8-size-too-large.png` — رفع `huge.png` بـ501 MB. mk-api يعيد 413
  `SIZE_TOO_LARGE` من `/v1/assets/upload-url`. البانر الأحمر يعرض
  «الملف أكبر من الحدّ المسموح.» — **مترجَم لا خام** (L-22 يعمل
  طرفاً إلى طرف).

**سطر يُكتب بسببه — `s8-svg-warning.png` ثم
`s8-svg-warning-acknowledged.png`:**
رفع ملف SVG يحوي نصّاً غير محوَّل. الخادم يعيد 400
`INVALID_SVG_WITH_TEXT_WARNING`. **هذا ليس فشلاً بل خطوة في التدفّق:**
- `s8-svg-warning.png` — بعد الفشل الأوّل، تنبيه أصفر بعنوان «تحذير
  SVG» + نصّ توضيحي عربي كامل + زرّ ذهبي «أقرّ وأكمل». لا بانر أحمر،
  لا رسالة فشل.
- `s8-svg-warning-acknowledged.png` — بعد الضغط على «أقرّ وأكمل»،
  الواجهة أعادت `finalize` بـ`acknowledgedWarnings: ['SVG_HAS_TEXT']`،
  الخادم عاد 200، النموذج فُرِّغ. تدفّق «تحذير → إقرار → متابعة»
  عمل بلا رسالة خطأ للمستخدم.

### الهويات + القوالب + إصدار الأصول (S9+S10+S11 · `60f81ba`)

مقابل A13 (Templates) + A14 (Projects, أُضيفت للمرآة استباقاً) +
A15+A16+A17 (Workflows/Annotations، للمرآة أيضاً).

**S11 القوالب:**
- `s9-11-templates-list.png` — القوالب الستة العامة الحقيقية من
  seed migration بأسماء عربية (بسيط · بطاقة ذات كيكر · بطاقة سفلية
  · بطاقة متمركزة · بطاقة عاجل · ريلز). كل صف يحمل شارة «للقراءة
  فقط» بجوار الاسم — **قبل** أي محاولة، لا انتظار لـ403.
- `s9-11-templates-global-readonly.png` — فلتر `scope=عام` يُبرز
  الشارات. تأكيد بصري لسياسة العرض قبل الفعل.
- `s9-11-templates-duplicate-visible.png` — بعد النسخ + فلتر
  `scope=خاص`، «بسيط — إثبات بوابة المرحلة 2 — نسخة» يظهر بشارة
  خضراء «خاص» + زرّ «حذف» ظاهر (يظهر فقط لـtenant، مخفي لـglobal).
  النسخ = GET :id + POST / بالتعريف (نقرة واحدة، طلبان تحتها — لا
  `/duplicate` في العقد).

**S9 الهويات + إقرار الترخيص:**
- `s9-fontack-success.png` — بعد التحقّق من checkbox + إرسال إقرار
  الخط لخط `Cairo Custom` (تمّ تجهيزه عبر PATCH ليكون
  `source='custom'`). الردّ 200 يحمل الجزء المُحدَّث فقط
  (`{fonts:{primary}}`)، الواجهة تعرض مربعاً أخضر بـ`أقرّ بواسطة:
  {userId}` و `تاريخ الإقرار: {ISO}` — read-only (§4.ب).
- `s9-fontack-422.png` — الإرسال بـcheckbox غير مؤشَّر. mk-api يعيد
  422 `LICENSE_ACK_MUST_BE_TRUE`، الواجهة تعرض «يجب الإقرار بالترخيص
  لإتمام الإجراء.» مترجَماً — لا خام.

**S10 إصدار الأصول:**
- `s10-version-success.png` — بعد الترقية، عمود «إصدار الأصول» في
  الصف الأوّل يعرض `2026.06` (كان `—` قبلها). الحوار أُغلق تلقائياً.

**سطر يُكتب بسببه — `s10-version-diff-step.png`، 409 كخطوة تدفّق:**
- الضغط الأوّل يرسل `acknowledgedDiff: false` عمداً. mk-api يعيد
  409 `DIFF_NOT_ACKNOWLEDGED`.
- الواجهة لا تعرضه **بانراً أحمر** ولا Alert خطأ. بدلاً منه تنتقل
  إلى step=`diff` وتعرض **تنبيهاً أصفر** «الفرق» + placeholder +
  checkbox «أقرّ بمراجعة الفرق أعلاه.» — نفس نمط SVG warning في S8.
- الضغط الثاني بعد التحقّق يرسل `acknowledgedDiff: true` → 200.
- 409 هنا **خطوة تصميم في UI**، ليس فشلاً يُبلَّغ للمستخدم.

### المشاريع + المحرّر + المراجعات + التصدير (S12 · `[current]`)

**بيئة اللقطات:** `NEXT_PUBLIC_API_MOCK=true`. Projects/Renders/
Revisions/Workflows خلف SYNC-δ في mk-api (غير مبنية بعد). المرآة
عقدياً مطابقة لـ`docs/16` §7 · §8 · §10 · §11. **الغاية:** إثبات
تدفّقات S12 كاملةً على أشكال العقد الرسمية، فتنفتح SYNC-δ يوماً
والواجهة تعمل بلا تعديل.

**لماذا mock هنا (وليس real mk-api)؟** الحقيقي يفتقر بالكامل إلى
endpoints §7/§8/§10/§11 الآن. لقطات على «خام mk-api ينقص endpoints»
= 404 على كل ضغطة = مادة عرض بلا قيمة. mock مبني على المرآة نفسها
التي تحرسها L-63.

**قائمة المشاريع:**
- `s12-list-empty.png` — القائمة قبل أيّ إنشاء، `EmptyState` مركزي
  «لا مشاريع بعد» + وصف عربي كامل + زرّ ذهبي «مشروع جديد» في الترويسة.
  فلاتر الحالة الخمسة (الكل · مسودّة · قيد المراجعة · معتمد · مؤرشف)
  ظاهرة قبل أن تُملأ القائمة (G-S12-3).
- `s12-list-populated.png` — بعد إنشاء مشروع «إثبات محرّر S12» بقالب
  «بطاقة عاجل» وهوية «هوية العرض». الجدول يعرض ٧ أعمدة: العنوان (رابط
  ذهبي) · الحالة («مسودّة» شارة رمادية) · الهوية · القالب · المُسنَد
  إليه («غير مُسنَد») · آخر تحديث (ISO مقتطع، dir=ltr كي يعمل)
  · حذف. G-S12-3 صحيح.

**إنشاء مشروع:**
- `s12-create-dialog.png` — الحوار يفتح على أربعة حقول: عنوان المشروع
  (`*` إلزامي) · الهوية (select من brand-kits list) · القالب (select
  من templates list) · لغة المحتوى (ar/en). لا حقول ثابتة في المكوّن
  — كلها من العقد. G-S12-4 صحيح.

**محرّر المحتوى (حقول مُشتقّة من `template.definition.fields`):**
- `s12-editor-loaded.png` — الترويسة تعرض عنوان المشروع + شارة الحالة
  + طابع `updatedAt`. الجانب الأيمن العريض يعرض قسم «المحتوى» — حقل
  واحد `title` مع علامة `*` (label من `template.definition.fields`،
  ليس مكتوباً في الكود). القالب المختار «بطاقة عاجل» يحمل حقلاً
  واحداً — إثبات أن المحرّر يشتق نموذجه من القالب، لا يفترضه.
  G-S12-5 صحيح.
- `s12-editor-dirty.png` — بعد كتابة نصّ في الحقل، زرّ «حفظ» صار
  ذهبياً (كان مُعطَّلاً قبل التعديل).
- `s12-editor-saved.png` — بعد النقر «حفظ»، Alert معلوماتي «تم الحفظ»
  ظهر أعلى المحرّر. PATCH نجح مع رأس `If-Match: <updatedAt القديمة>`
  → استجابة 200 بـupdatedAt جديد يُحدَّث state (G-S12-6 صحيح ضمنياً
  — إن غاب If-Match على الخادم الحقيقي، سيعود 428 `IF_MATCH_REQUIRED`
  والواجهة تعرض «الطلب رفض بلا رأس If-Match…»).

**التصدير — polling حقيقي:**
- `s12-render-queued.png` — بعد النقر «تصدير الآن» في حالة `draft`،
  الجدار يعرض «أُرسل التصدير — في الانتظار...» ثم «قيد التنفيذ...».
- `s12-render-ready.png` — بعد ~3 ثوانٍ (mock يحاكي مسار queued →
  running → succeeded)، الجدار يعرض «المخرج جاهز — افتح للتحميل»
  كرابط ذهبي إلى `mock://output/<id>.png`. G-S12-10 صحيح.

**سير العمل — تحوّلات من `availableTransitions`:**
- `s12-editor-after-transition.png` — بعد النقر على السهم بجوار
  «إرسال للمراجعة» (transition `trn_submit`)، حالة المشروع صارت
  «قيد المراجعة». قسم سير العمل الآن يعرض تحوّلَين متاحَين من الحالة
  الجديدة: «إرجاع للتحرير» (يطلب سبباً) و «اعتماد» (لا يطلب).
  زرّ «تصدير الآن» ظاهر لكن Alert أحمر تحته «التصدير غير مسموح في
  الحالة الحالية للمشروع.» — mk-api-facing 403 `RENDER_NOT_ALLOWED_IN_
  CURRENT_STATE` (§8.1). G-S12-7 صحيح.
- `s12-transition-reason.png` — بعد إدخال «يحتاج مراجعة إملائية على
  العنوان» في textarea التحوّل «إرجاع للتحرير» (لأن `requiresReason:
  true`). النصّ ظاهر داخل textarea يمين السهم. الضغط على السهم بلا
  سبب سيعرض «errors.REASON_REQUIRED_FOR_THIS_TRANSITION» مترجَماً
  (§11.7).

**سجل المراجعات + الاستعادة:**
- `s12-revisions-list.png` — بعد فتح «سجل المراجعات»، جدول بأربعة
  أعمدة (وقت التغيير · المُغيِّر · العملية · لقطة) يعرض:
  - تعديل (update) 00:25:09 · usr_mock — من الحفظ.
  - إنشاء (insert) 00:25:06 · usr_mock — من إنشاء المشروع، مع
    شارة `•` خضراء تشير إلى وجود snapshot كامل (§10.1
    `hasSnapshot`).
  خلف الحوار: قسم التاريخ يعرض `draft → review · 2026-09-06 00:25:18`.
  G-S12-8 + G-S12-11 صحيحان.
- `s12-restore-dialog.png` — النقر «استعادة» على أيّ صف يفتح حواراً
  ثانياً: «استعادة إلى هذه المراجعة» + textarea «سبب الاستعادة
  (١٠ أحرف على الأقل — L-15)» + زرّ «استعادة الآن» أحمر. الضغط بسبب
  <10 محارف يفشل بـ400 `REASON_TOO_SHORT`. الضغط بسبب يحوي كلمة
  `break` يفشل بـ409 `RESTORE_WOULD_BREAK_REFERENCES` (§10.3
  — mock trigger).

**حاجز الهوية الخارجية — G-S12-9:**
- `s12-external-blocked.png` — مشروع جديد بهوية `bk_mock_external`
  (اسمها «هوية بأصول خارجية»). النقر «تصدير الآن» يعود بـ422
  `UNSUPPORTED_BRAND_HAS_EXTERNAL_ASSETS` (§8.1). الواجهة لا تعرضه
  Alert أحمر بل **بلوحة صفراء (warning)** بنصّ مترجَم كامل:
  «الهوية تحوي أصولاً خارجية غير مدعومة — يجب معالجتها قبل التصدير.»
  السلوك مطابق للنمط الذي أُثبت في S8 (SVG_HAS_TEXT) و S10
  (DIFF_NOT_ACKNOWLEDGED): 4xx ليس بالضرورة رسالة فشل — قد يكون
  تنبيهاً بشرطٍ يعالجه المستخدم في مكان آخر.
