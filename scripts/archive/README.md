# scripts/archive/ — سكربتات تحقيق تاريخيّ، ليست بوابات

هذا المجلد يحمل سكربتات كُتبت كأدوات تحقيق (investigation) في وقت
ما، لم يدّعِها أحد بوابةً في PHASES ولا رُبطت بمفتاح `pnpm`. تُترك
هنا للاستئناس التاريخي، لا للتشغيل الآلي.

**قاعدة عامّة (WIRE-7 · L-71):** سكربت بلا مفتاح ليس بوابة. حارس غير
موصول أسوأ من غياب حارس لأنّه يشتري طمأنينة كاذبة.

---

## `verify-boundary-identity.mjs`
- **تاريخ الأرشفة:** 2026-09-10 (WIRE-7)
- **السبب:** لا ذكر في `PHASES.md` بأيّ نتيجة كمّية · لا مفتاح في
  `package.json` · لا استشهاد به من أيّ اختبار.
- **السلوك عند التشغيل:** يفشل حالياً بـ`ERR_UNKNOWN_FILE_EXTENSION`
  على استيراد `.ts` من `@pf-mediakit/shared` (يحتاج `--import tsx`).
  بعد `tsx` قد يعمل، لكن لا وثيقة تفسّر ما يقيسه ولا مالك يدّعيه.

## `verify-wrap-fidelity.mjs`
- **تاريخ الأرشفة:** 2026-09-10 (WIRE-7)
- **السبب:** نفس السبب — بلا ذكر في PHASES بلا مفتاح بلا مالك.
- **السلوك عند التشغيل:** نفس عطب `.ts` بلا `tsx`.

---

**قاعدة الحذف مقابل الأرشفة:** لا تُحذف — إن احتاج أحد استئناساً
تاريخيّاً، يجدها هنا. إن أراد إحياءها بوابةً: يُضاف مفتاح، يُضاف
اختبار سلبيّ صريح (L-46)، يُدخَل `pnpm test`، ثمّ يُنقل خارج
`archive/`. لا وسيط.
