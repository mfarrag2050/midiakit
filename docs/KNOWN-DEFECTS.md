# docs/KNOWN-DEFECTS.md — مصدر عطب مسار العرض

> **مصدر آليّ لبوّابة الترقية** — `bin/mk show gate <sha>` يقرأ هذا الملفّ ويعدّ الأسطر التي تبدأ بـ`- [ ]` (خانة غير محدَّدة). أيّ سطر مفتوح يُسقط شرط #4 من البوّابة.
>
> **قاعدة تحرير:**
> - عطب جديد يمنع ترقية العرض → أضِف سطراً: `- [ ] وصف موجز · تاريخ · أثره على مسار العرض · مَن سيغلقه`
> - عطب أُغلق → غيّر `[ ]` إلى `[x]` واحتفظ بالسطر (للتاريخ).
> - قسم «تلاحظ» للعطب المعروف الذي **لا** يمنع الترقية (مثل: pre-existing tech debt).

## عطب مفتوح (يمنع الترقية)

<!-- ابدأ الأسطر بـ`- [ ] `. صفر أسطر = صفر عطب مفتوح = الشرط يمرّ. -->
- [x] (لا عطب معلَن حالياً · 2026-09-13)

## تلاحظ (لا يمنع الترقية)

- [x] apps/studio: `next build` يفشل بـ`useContext=null` في prerender — الشوروم يستعمل `next dev` كـfallback في bin/mk-show. مسجَّل في `_AMEND-SHOWROOM-PORTS §L-46 GREEN`. يُغلَق حين تُصلحه mkst.
