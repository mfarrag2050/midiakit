# INVENTORY — جرد ما يجب إزالته من `reference/aa-media-kit.html`

> **ملف تاريخي:** المرحلة 0 (التنظيف القانوني) أُغلقت — الحذف تمّ في
> commit `de03a99` («phase 0 — remove AA assets, swap commercial font,
> add inventory»). هذا الجرد يوثّق ما كان في الأداة **قبل** التنظيف،
> بأرقام أسطر النسخة قبل الحذف (`ee178ca`). يُحفَظ للرجوع، لا للتنفيذ.
>
> عدد أسطر الملف عند `ee178ca`: **2629**.

---

## 1. أصول base64 مضمّنة (أناضول)

كل ثابت هو **سطر واحد طويل** (البيانات مكتوبة داخل السطر). البداية = النهاية.

| المعرّف | السطر | الغرض في الأداة | الحجم التقريبي |
|---|---|---|---|
| `AA_LOGO_MAIN` | **645** | شعار AA الرئيسي المعروض على البطاقات (`cvLogo` في `cvRenderInto`) | ~10.5KB base64 |
| `AA_LOGO_486` | **646** | شعار AA لصور 486 (تراجع لـ `cvLogo`) | ~7.9KB base64 |
| `AA_LOGO_978` | **647** | شعار AA لصور 978 (تراجع لـ `cvLogo`) | ~7.9KB base64 |
| `CV_LOGO` | **1482** | شعار العلامة المستخدم في العرض التجريبي (تُبنى منه صورة `cvLogo` كشعار افتراضي) | ~2.3KB base64 |

**استخدامات الأصول (يجب إزالتها أو استبدالها):**

- `AA_LOGO_MAIN` — يُحمَّل في السطر **1203** إلى `cachedLogo`
- `AA_LOGO_486` — يُحمَّل في السطر **1204** إلى `cachedLogo486`
- `AA_LOGO_978` — يُحمَّل في السطر **1205** إلى `cachedAALogo978`
- `CV_LOGO` — يُسند إلى `cvLogo.src` في السطر **1483**
- `cvLogo` يُستخدم في الرسم:
  - `cvBreakingBg` — السطر **1875–1882** (شعار مائي داخل خلفية العاجل)
  - `cvRenderInto` — السطر **1955** (شعار سفلي على كل البطاقات)
  - `cvDrawBrkVideoOverlay` — السطر **2115** (شعار سفلي على الفيديو)

---

## 2. مراجع الخط التجاري `HelveticaNeueLTArabic`

### إعلان الخط (@font-face)

| السطر | الوزن | المصدر |
|---|---|---|
| **214** | 700 | `HelveticaNeueLTArabic-Bold.ttf` |
| **215** | 400 | `HelveticaNeueLTArabic-Roman.ttf` |
| **216** | 300 | `HelveticaNeueLTArabic-Light.ttf` |

### مراجع الأسرة `'HNArabic'` (اسم مستعار داخلي للخط التجاري)

**في CSS للواجهة:**
- السطر **233**: `#cvHeadline, #cvKicker, #cvSource`
- السطر **306**: `.cv-seg-h, .cv-seg-s`
- السطر **316**: `#rlTitle`
- السطر **317**: `#rlLoc`

**في كود الرسم على Canvas (`ctx.font = '… HNArabic'`):**
- السطر **1587** — `document.fonts.load('700 80px HNArabic')` و `'300 44px HNArabic'`
- السطر **1756** — داخل `cvWrapFit`
- السطر **1782** — `cvWordWidth`
- السطر **1783** — `cvSpaceWidth`
- السطر **1807** — `cvDrawLine`
- السطر **1847** — `cvDrawLineRightEdge`
- السطر **1857** — `cvBadge`
- السطر **1937** — `cvRenderInto` (نمط `s3l`، خط الـkicker)
- السطر **1952** — `cvRenderInto` (نمط `brk`، نص المصدر)
- السطر **1959** — `cvRenderInto` (علامة العيّنة `SAMPLE`)
- السطر **2111** — `cvDrawBrkVideoOverlay` (نص المصدر في الفيديو)
- السطر **2446** — `rlBadge` (شارة الريلز)
- + استخدامات إضافية داخل عرض الريلز (`rlDrawTitle` وما بعدها بعد السطر 2450) — يجب استبدالها بالبحث الشامل.

**عدد الاستخدامات الإجمالية لسلسلة `HNArabic`:** يُبدَّل الاسم كاملاً في مرحلة الحذف بحيث لا يتبقى أي مرجع.

---

## 3. مراجع الصوت

### `AUDIO_TRACKS` (تعريف الجدول)

| السطر | المحتوى |
|---|---|
| **1999–2001** | `const AUDIO_TRACKS = [ { file:'audio/AA-60-SANIYE-2021_01.wav', label:'AA 60 sn' } ];` |

### مسار الملف `AA-60-SANIYE-2021_01.wav`

- السطر **2000** فقط. الملف نفسه غير موجود في المستودع (مسار افتراضي).

### الاستخدامات

- السطر **2012** — `cvPopulateAudio` (تعبئة قائمة الصوت في الأداة)
- السطر **2620** — تعبئة قائمة الصوت في محرّر الريلز

---

## 4. القيم اللونية المثبتة داخل دوال الرسم

> **معيار الفحص:** كل استدعاء `ctx.fillStyle = '...'` أو `ctx.strokeStyle = '...'` داخل دالة رسم على Canvas. تُستثنى ألوان CSS للواجهة (`background`, `border` … إلخ).

### قيم موجودة في المواصفة (`docs/03-brand-kit-spec.md`) وتُطابقها

| اللون | السطر | الدالة | مطابقة المواصفة |
|---|---|---|---|
| `#474c55` | **1738** | `cvPlaceholderBg` (بداية التدرّج) | ✔ `colors.placeholder[0]` (رغم أن المواصفة تكتبها `#474C55` بأحرف كبيرة — نفس القيمة) |
| `#15171b` | **1738** | `cvPlaceholderBg` (نهاية التدرّج) | ✔ `colors.placeholder[1]` |
| `#B1876A` | **1765** | `cvAccent` (خط التمييز الأفقي) | ✔ `colors.accent` |
| `#B1876A` | **1766** | `cvAccentSpan` (خط التمييز الممتد) | ✔ `colors.accent` |
| `#C1012F` | **1860** | `cvBadge` (خلفية شارة عاجل) | ✔ `colors.urgentBadge` |
| `#C82626` | **1874** | `cvBreakingBg` (خلفية العاجل بلا صورة) | ✔ `colors.urgentBg` |
| `#C21C1C` | **1881** | `cvBreakingBg` (تلوين الشعار المائي) | ✔ `colors.urgentBgTint` |
| `#1D5FC4` | **2331** | `const RL_BLUE` (شارة موقع الريلز) | ✔ `colors.locationBadge` |

### قيم مثبتة **غير موثّقة** في المواصفة

| اللون | السطر | الدالة | ملاحظة |
|---|---|---|---|
| `#111` | **1327** | إنشاء thumbnail عند رفع صورة | خلفية داكنة للـcanvas قبل رسم الصورة. **يجب** أن يخرج إلى `brand.colors.surface`. المواصفة عرّفت `surface: "#111111"` — التطابق موجود لكن القيمة مكرّرة مباشرة. |
| `#111` | **1893** | `cvRenderInto` (خلفية البطاقة قبل رسم أي شيء) | نفس ما سبق — `brand.colors.surface`. |
| `#111` | **2435** | `rlDrawItemBg` (خلفية عنصر ريلز فارغ) | نفس ما سبق. |
| `#fff` | **1804** | `cvDrawLine` (لون النص الأبيض) | يجب أن يخرج إلى `brand.colors.text`. المواصفة عرّفت `text: "#FFFFFF"`. |
| `#fff` | **1845** | `cvDrawLineRightEdge` | نفس الملاحظة. |
| `#fff` | **1861** | `cvBadge` (نص شارة عاجل) | يجب أن يخرج إلى `brand.badges.urgent.textColor` (المواصفة توثّقه). |
| `#fff` | **1937** | `cvRenderInto` (نص الـkicker) | `brand.colors.text`. |
| `#fff` | **1952** | `cvRenderInto` (نص المصدر) | `brand.colors.text`. |
| `#fff` | **2111** | `cvDrawBrkVideoOverlay` (نص مصدر الفيديو) | `brand.colors.text`. |
| `#fff` | **2450** | `rlBadge` (نص شارة موقع الريلز) | يجب أن يخرج إلى `brand.badges.location.textColor` (المواصفة توثّقه). |
| `rgba(0,0,0,0.45)` | **1961** | `cvRenderInto` — خلفية علامة `SAMPLE` | مؤقتة/تجريبية؛ ليست من الهوية. تُنقل لاحقاً إلى `debug.sampleBadge` أو تُحذف مع علامة العيّنة. |
| `rgba(255,255,255,0.9)` | **1962** | `cvRenderInto` — نص علامة `SAMPLE` | نفس الملاحظة. |
| `rgba(20,30,68,.62)` | **2464** | `rlDrawTitle` — `shadowColor` لعنوان الريلز | المواصفة توثّقه في `shadows.reelTitle`، لكن الكود يستعمل قيمة مثبتة (`,.62`) بينما المواصفة كتبت `,0.62` — نفس القيمة عملياً. |

### قيم CSS (واجهة الأداة، ليست دوال رسم)

- السطور **1870**, **2351**: `el.style.color = '#059669' / '#d97706'` — تلوين رسالة عدّاد الكلمات في نموذج HTML، ليس Canvas.
- كل القيم في نطاق السطور **15–450** هي CSS للواجهة الرمادية للتطبيق نفسه (زر، إطار، خلفية لوحة … إلخ). خارج نطاق فحص «قيم داخل دالة رسم».

---

## 5. الفرق بين ما وُجد وما وثّقته المواصفة

| البند | المواصفة | ما وُجد فعلاً | الحكم |
|---|---|---|---|
| `#B1876A` accent | موثّق | مطابق | ✔ |
| `#C1012F` urgentBadge | موثّق | مطابق | ✔ |
| `#C82626` urgentBg | موثّق | مطابق | ✔ |
| `#C21C1C` urgentBgTint | موثّق | مطابق | ✔ |
| `#1D5FC4` locationBadge | موثّق | مطابق (تحت اسم `RL_BLUE`) | ✔ |
| `#474C55 → #15171B` placeholder | موثّق | مطابق (بأحرف صغيرة) | ✔ فرق شكلي فقط |
| `#FFFFFF` text | موثّق | يظهر كـ `#fff` في 7 مواضع | ✔ نفس القيمة |
| `#111111` surface | موثّق | يظهر كـ `#111` في 3 مواضع | ✔ نفس القيمة |
| `rgba(20,30,68,0.62)` reelTitle shadow | موثّق | `rgba(20,30,68,.62)` | ✔ نفس القيمة |
| `#059669` / `#d97706` | **غير موثّق** | استخدام CSS فقط لعدّاد الكلمات في الواجهة | ⚠ خارج المحرك — لا داعي لإضافته للـBrand Kit |
| `rgba(0,0,0,0.45)` سامبل | **غير موثّق** | علامة عيّنة تجريبية | ⚠ يُحذف أو يُنقل إلى `debug` |

**لا توجد قيمة لونية مثبتة داخل دوال الرسم غير مغطاة بالمواصفة**، باستثناء علامة SAMPLE (تجريبية) وألوان عدّاد الكلمات (خارج المحرك أصلاً).

---

## 6. ملخّص ما يُحذف على `main`

| البند | المصدر | الإجراء |
|---|---|---|
| ثوابت `AA_LOGO_MAIN/486/978` | 645–647 | حذف كامل |
| ثابت `CV_LOGO` وتعيين `cvLogo.src` | 1482–1483 | حذف كامل |
| تحميل `cachedLogo/486/978` | 1203–1205 | حذف كامل + جعل `cvLogo` مصدره فارغاً (شعار محايد لاحقاً) |
| `@font-face 'HNArabic'` لـHelveticaNeueLTArabic | 214–216 | استبدال بـ IBM Plex Sans Arabic (Google Fonts) |
| كل `HNArabic` في CSS و Canvas | 15+ موضع | إعادة تسمية إلى `IBMPlexArabic` |
| `AUDIO_TRACKS` + `AA-60-SANIYE-2021_01.wav` | 1999–2001 | تفريغ المصفوفة (تبقى بنية `AUDIO_TRACKS = []`) |

**بعد الحذف** لا يجب أن يُظهر أي بحث نصي في `main`:

- `AA_LOGO` · `CV_LOGO` · `HelveticaNeueLTArabic` · `HNArabic` · `AA-60-SANIYE` · `anadolu`

---

## 7. ما يبقى خالياً للـBrand Kit اللاحق (المرحلة 1)

القيم اللونية المثبتة أعلاه (`#B1876A`, `#C1012F`, `#C82626`, `#C21C1C`, `#1D5FC4`, `#474c55`, `#15171b`, `#111`, `#fff`) **لا تُحذف الآن** — الأداة يجب أن تعمل. تُترك كما هي حتى المرحلة 1 حين يستخرجها المحرك إلى `brandKit`. هذا الجرد يوثّق أين هي بالضبط لتسهيل ذلك النقل لاحقاً.
