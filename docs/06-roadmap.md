# 06 — خارطة الطريق

منهج **الخنق التدريجي**: الأداة الحالية تبقى تعمل أثناء استخراج المحرك منها. لا إعادة كتابة من الصفر.

---

## المرحلة 0 — التنظيف القانوني (أسبوعان)

لا يُكتب سطر واحد من كود المنتج قبل انتهاء هذه المرحلة.

- [ ] إزالة `HelveticaNeueLTArabic-*.ttf` من أي مستودع تجاري
- [ ] تقييم الخطوط المفتوحة: IBM Plex Sans Arabic / Almarai / Tajawal / Cairo / Noto Kufi Arabic — أيها أقرب وزناً بصرياً للحالي
- [ ] إزالة `AA_LOGO_MAIN`, `AA_LOGO_486`, `AA_LOGO_978`, `CV_LOGO` (base64)
- [ ] إزالة `AA-60-SANIYE-2021_01.wav`
- [ ] فصل فرعين: `aa-internal` (يبقى للوكالة كما هو) و `main` (المنتج التجاري)
- [ ] صياغة بند إقرار ترخيص الخطوط في شروط الخدمة

**المخرج:** الأداة تعمل بخط مفتوح وشعار محايد.

---

## المرحلة 1 — استخراج المحرك (3–4 أسابيع)

**الهدف: تشغيل الأداة نفسها بهويتين مختلفتين دون لمس كود الرسم.** هذه أصعب مسافة في المشروع كله؛ ما بعدها هندسة عادية.

- [ ] `packages/engine` بـ TypeScript، إعداد الحزم (pnpm workspaces)
- [ ] نقل طبقة النص: `parseTokens`, `wrapAlternating`, `layoutBalanced`, `Measurer`, `drawLineRTL`
- [ ] بناء `BrandKit` كامل من القيم المستخرجة في ملف 03
- [ ] `DEFAULT_BRAND` محايد
- [ ] تفكيك `cvRenderInto` إلى مفسّر طبقات
- [ ] `loadBrandFonts` مع انتظار الجاهزية قبل أي قياس
- [ ] ربط الواجهة الحالية بالمحرك الجديد — **بلا أي تغيير مرئي للمستخدم**
- [ ] **BiDi**: `splitBidiRuns` + `orderRuns` + `mapNumerals` — أي اسم لاتيني وسط عنوان عربي (ملف 09)
- [ ] لقطات مرجعية للقوالب الأربعة

**معيار النجاح:** تبديل `brand` من كونسول المتصفح يغيّر المخرج بالكامل، وعنوان عربي بكلمة لاتينية يظهر بترتيب صحيح.

---

## المرحلة 2 — القوالب بيانات (2 أسابيع)

- [ ] JSON Schema للقالب + مُتحقق
- [ ] تحويل القوالب الأربعة إلى JSON (ملف 04)
- [ ] مفسّر الطبقات يقرأ JSON بدل الدوال
- [ ] حل المراجع `brand.*` عبر `resolve()`
- [ ] إثبات المفهوم: إضافة قالب خامس **بملف JSON فقط**

---

## المرحلة 3 — الرندر على الخادم (3 أسابيع)

- [ ] `apps/renderer` بـ Node + `skia-canvas`
- [ ] تشغيل `renderFrame` من سطر الأوامر ← PNG. **هذا اختبار صحة المعمارية كلها.**
- [ ] مقارنة بكسلية: مخرج Node مقابل مخرج المتصفح
- [ ] **أنبوب مباشر** إلى FFmpeg (`-f rawvideo -i pipe:0`) — لا إطارات على القرص
- [ ] BullMQ بأربعة طوابير: `urgent` / `normal` / `edit` / `batch` (طابور `edit` يُفعّل في المرحلة 3.7)
- [ ] `concurrency = floor(cores/2)` + حصة عادلة على `tenant_id`
- [ ] التحقق من المقاطع **قبل** دخول الطابور + مهلة 30 ثانية للعاجل
- [ ] تنظيف في `finally` + حدود صارمة (90s · 8 مقاطع · 500MB)
- [ ] **WebCodecs** للفيديو القصير في المتصفح، والخادم كتراجع
- [ ] حذف `MediaRecorder` نهائياً

**المخرج:** فيديو 60 ثانية يُرندر في أقل من 60 ثانية، بنتيجة متطابقة على أي جهاز.

**معيار قبول الذروة (إلزامي):** تسع مهام متزامنة ⇒ لا مهمة عاجلة تتجاوز 45 ثانية للبدء. تفاصيل ملف 08.

---

## المرحلة 3.2 — لوحات التحكم (أسبوعان)

عملاء من خلفية الجزيرة سيختبرون النظام بحمل حقيقي. المطلوب رؤية وثقة لا مرونة فعلية (ملف 08).

- [ ] لوحة العميل: موقع الطابور + الزمن المتوقع + سجل التصديرات + الاستهلاك
- [ ] لوحة التشغيل: عمق الطوابير، المهام النشطة، الفشل، الموارد، العتبات
- [ ] إجراءات يدوية: إيقاف مهمة · إعادة تشغيل عامل · `WORKER_COUNT` · وضع صيانة
- [ ] تنبيهات تيليجرام: فشل · قرص > 80% · طابور > 10 · عامل معلّق
- [ ] رقم طاقة معلن + التزام زمني في العقد

---

## المرحلة 3.7 — محرّر الخط الزمني (6–8 أسابيع)

**قرار المالك: كامل من الإصدار الأول.** المواصفة الكاملة في `10-timeline-editor.md`، والمهام في `PHASES.md`.

سبع مراحل فرعية: النواة الزمنية · مسار الوسائط · مسارات النص · الانتقالات · الصوت · الواجهة · الرندر.

**البوابة:** مشروع بأربعة مسارات يُصدَّر MP4 مطابقاً للمعاينة، وبطاقة العاجل لا تتأخر خلفه.

---

## المرحلة 4 — المنصة (4–6 أسابيع)

- [ ] `apps/studio` بـ Next.js
- [ ] مصادقة + مستأجرون + أدوار
- [ ] محرر Brand Kit (ألوان، خط، شعار، معاينة حية)
- [ ] رفع الأصول مع إقرار الترخيص
- [ ] حفظ المشاريع + سجل التصديرات
- [ ] الاشتراكات (Stripe / Paddle) + الحصص
- [ ] عزل المستأجرين على مستوى قاعدة البيانات (RLS)

---

## المرحلة 3.5 — الخندق التنافسي (3–4 أسابيع)

تُنفَّذ **مباشرة بعد الرندر على الخادم وقبل بناء المنصة**. السبب في ملف 07: هذه ليست تحسينات طباعية بل فجوات تقنية عمرها عشرون عاماً لا يستطيع أي منافس يعمل على HTML سدّها.

- [ ] **التبرير بالكشيدة** — المسار (أ): إدراج محرف التطويل بحساب مواضع الوصل
- [ ] كشف قابلية الخط للتطويل عند الرفع، وتخزين النتيجة في `brandKit`
- [ ] **كسر السطور الدلالي** — طبقة القواعد العربية أولاً، دالة `breakPenalty`
- [ ] `lineHeight` ديناميكي محسوب من الارتفاع الفعلي للسطر
- [ ] **التشكيل الآلي** عبر نموذج مفتوح داخل `apps/renderer`
- [ ] لقطة مقارنة: مخرجك مقابل مخرج Canva لنفس العنوان — **هذه أداة البيع**

**معيار النجاح:** عنوان عربي بحواف مستقيمة، بلا كسر داخل وحدة معنى، مع تشكيل اختياري لا يتصادم مع السطر الأعلى.

---

## المرحلة 3.7 — محرّر الخط الزمني (6–8 أسابيع)

**قرار المالك: كامل من الإصدار الأول.** المواصفة الكاملة في `10-timeline-editor.md`، والمهام في `PHASES.md`.

سبع مراحل فرعية: النواة الزمنية · مسار الوسائط · مسارات النص · الانتقالات · الصوت · الواجهة · الرندر.

**البوابة:** مشروع بأربعة مسارات يُصدَّر MP4 مطابقاً للمعاينة، وبطاقة العاجل لا تتأخر خلفه.

---

## المرحلة 4 — المنصة (4–6 أسابيع)

(كما هي أعلاه)

## المرحلة 5 — ما يجعلهم يدفعون سنوياً

مرتبة حسب الأثر مقابل الجهد بعد المسح:

- [ ] **التوليد من مصدر بيانات** — لصق رابط خبر أو ربط RSS من نظام إدارة المحتوى. هذا الفرق بين أداة تصميم وجزء من سير عمل غرفة الأخبار
- [ ] **قوالب الأرقام والاقتباس ونتائج المباريات** — أكثر ما يُنشر عربياً بعد العاجل
- [ ] **العنوان في ثلاثة أطوال** — صياغة تلقائية لكل مقاس
- [ ] **توليد بالجملة** — CSV بعشرين عنواناً ← عشرون بطاقة
- [ ] **تدقيق ما قبل التصدير** — تباين، منطقة آمنة، عدد كلمات، وجه تحت النص
- [ ] **سجل ومراجعة** — مصمّم يُنتج، مسؤول يعتمد
- [ ] **API عام** بمفاتيح وحصص
- [ ] تكاملات: Buffer / Later / Zapier / Slack
- [ ] إعادة Photopea كإضافة اختيارية

---

## الترتيب الحرج

**0 ← 1 ← 3 ← 3.5 ← 3.7** هو العمود الفقري. عند اكتمالها لديك عرض حي أمام أول عميل: هويتان مختلفتان، بطاقة وفيديو، MP4 نظيف، **وطباعة عربية لا يملكها منافس**. المرحلتان 2 و4 تجعلانه قابلاً للبيع بلا تدخّلك، و5 تجعله قابلاً للنمو.

تغيير مهم عن النسخة الأولى: **الكشيدة صعدت من "تحسين لاحق" إلى المرحلة 3.5.** هي لافتة المنتج لا تفصيله.

## ما لا يجب فعله

- لا تبدأ بالواجهة الجديدة قبل المحرك
- لا تبدأ بالحسابات والاشتراكات قبل أن يعمل الرندر على الخادم
- لا تعد كتابة منطق اللف العربي — انقله كما هو ثم حرّر ثوابته
- لا تضف قوالب جديدة قبل المرحلة 2
