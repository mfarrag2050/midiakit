node_modules/
.env*
!.env.example
dist/
out/
.tmp/
*.log
.DS_Store
*.tsbuildinfo

# مواد أصلية ضخمة — لا تُتَتبَّع (194MB GeoNames، ملفات RSS متغيّرة).
# الملفات المُشتقّة (data/external/*.json) هي التي تُتَتبَّع.
data/external/raw/
data/external/rss/
